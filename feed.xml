<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://recoma96.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://recoma96.github.io/" rel="alternate" type="text/html" /><updated>2025-07-12T23:21:45+00:00</updated><id>https://recoma96.github.io/feed.xml</id><title type="html">Recoma In DEV</title><entry><title type="html">Redis에서의 분산락 사용법 (1) - 분산락의 사용 이유와 주의할 점</title><link href="https://recoma96.github.io/distributed_lock/2025/06/20/python-redis-distributed-1.html" rel="alternate" type="text/html" title="Redis에서의 분산락 사용법 (1) - 분산락의 사용 이유와 주의할 점" /><published>2025-06-20T19:00:00+00:00</published><updated>2025-06-20T19:00:00+00:00</updated><id>https://recoma96.github.io/distributed_lock/2025/06/20/python-redis-distributed-1</id><content type="html" xml:base="https://recoma96.github.io/distributed_lock/2025/06/20/python-redis-distributed-1.html"><![CDATA[<h1 id="배경">배경</h1>

<p>약 2주 전, 회사 내에서 꽤 심각한 이슈가 발생했다. CeleryBeat 기반의 배치 프로세스가 동작 중 서버 리빌드로 인해 프로세스가 일시적으로 종료되었고, 이 과정에서 삭제되어야 할 분산락 캐시 데이터가 그대로 남게 되었다. 그 결과, 다음 배치 프로세스가 분산락을 확인하고 “작업이 아직 진행 중”이라 판단하며 실행되지 않는 문제가 생겼다.</p>

<p>다행히 문제는 해결됐지만, 이를 계기로 분산락 사용에 대해 다시 생각하게 되었고, 그 내용을 정리해 보려 한다.</p>

<h2 id="상세">상세</h2>

<p>회사 내 결제 서비스에서는 외부 업체로부터 카드 사용 승인을 받아야 한다. 어느 날, 이 승인 절차가 갑자기 작동하지 않기 시작했다. 보통은 외부 API 문제라면 하루 이내에 해결되는데, 이번엔 3일이 지나도 해결되지 않았다.</p>

<p>문제는 외부 API가 아니라 내부에 있었다. 디버깅 과정에서, 작업 후 삭제되어야 할 분산락 캐시 데이터가 남아 있었고, 이 데이터를 삭제하자 승인 프로세스가 정상적으로 작동하기 시작했다.</p>

<p>즉, <strong>분산락이 남아 있어 이후 작업이 영원히 대기 상태에 빠졌던 것이다.</strong></p>

<h1 id="분산락-distributed-lock-이란">분산락 (Distributed Lock) 이란?</h1>

<h2 id="1-이걸-왜-사용하나요">1. 이걸 왜 사용하나요?</h2>

<p>컴퓨터공학과 출신 또는 비전공이지만 CS를 공부해 봤다면 “동시성”이란 단어를 한번 씩 들어봤을 것이다. 그리고 그 다음에 생각나는 단어는 <code class="language-plaintext highlighter-rouge">mutex</code>라는 단어를 생각할 것이다. 그렇다면 이런 의문이 들을 것이다. 동시성 제어를 할 거면 <code class="language-plaintext highlighter-rouge">mutex</code>를 쓰면 되지 굳이 왜 분산락 얘기가 나오나요?</p>

<p><code class="language-plaintext highlighter-rouge">mutex</code>는 <strong>단일 프로세스 내에서만 유효하다.</strong> 프로세스 간에는 메모리를 공유하지 않기 때문에, 여러 서버나 인스턴스에서 동시에 접근하는 상황에서는 무용지물이다.
이럴 때, 다중 시스템 간 자원 접근을 제어하기 위한 방법이 필요하며, 그 역할을 하는 것이 분산락이다.</p>

<h2 id="2-어디에-사용하나요">2. 어디에 사용하나요?</h2>

<ul>
  <li>무중단 배포, 대규모 트래픽 대응을 위해 여러 인스턴스를 띄울 때 발생하는 동시성 이슈 방지</li>
  <li>멀티프로세스를 통한 병렬 처리 시, 공통 자원(파일, 메모리 등)에 잘못된 값이 저장되는 문제 방지</li>
</ul>

<p>제약조건(예: UNIQUE)으로 해결할 수 있는 경우도 있지만, 구조적으로 제약조건을 걸 수 없는 상황에서는 분산락이 실질적인 해결책이 된다.</p>

<h2 id="3-redis-기반의-분산락">3. Redis 기반의 분산락</h2>

<p>분산락을 사용하는 방법은 여러가지가 있으나 여기서는 Redis 기반의 분산락에 대해서 설명을 한다. 분산락에 있어서 가장 많이 쓰이는데 주된 이유는 다음과 같다.</p>

<ul>
  <li>RDB 처럼 디스크 접근이 아닌 메모리 접근으로 속도가 상대적으로 빠르다.</li>
  <li>Redis는 싱글 스레드 방식으로 요청을 받는다. 즉, 아무리 동시에 요청한다 해도 Redis는 <strong>무조건 순차적으로 요청을 받고 데이터를 관리하는 것이 보장이 된다.</strong> 즉, 동시접근을 100% 할 수 없다는 이야기다.</li>
</ul>

<h3 id="setnx">setnx?</h3>

<p>과거에는<code class="language-plaintext highlighter-rouge">SETNX</code> 명령어를 사용해 락을 구현하곤 했다</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SETNX mykey "Hello"  # 성공하면 1 반환
SETNX mykey "World"  # 이미 존재하면 0 반환
</code></pre></div></div>

<p>하지만 <code class="language-plaintext highlighter-rouge">SETNX</code>는 이미 deprecated 되었으며, 가장 큰 문제는 만료 기한 설정이 불가능하다는 것이다. 락을 획득한 후 프로세스가 예기치 않게 종료되면, 해당 키가 무한히 남아 시스템을 교착 상태에 빠뜨릴 수 있다. 공식 사이트 조차도 이 명령어 대신 <code class="language-plaintext highlighter-rouge">SET</code>을 사용하는 것을 권장하고 있다.</p>

<h3 id="대신-set-nx-ex를-사용하자">대신 SET NX EX를 사용하자.</h3>

<p>그래서 <code class="language-plaintext highlighter-rouge">SETNX</code> 대신 <code class="language-plaintext highlighter-rouge">SET</code>을 권장하고 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SET hello "world" NX EX 60
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">NX</code> 플래그를 사용해 <code class="language-plaintext highlighter-rouge">SETNX</code>와 동일한 기능을 낼 수 있고. 추가로 <code class="language-plaintext highlighter-rouge">EX</code>를 통해 유효 기간을 설정할 수 있다. 유효기간을 설정하게 되면, 어떠한 이슈로 프로세스가 Shutdown이 되더라도, 특정 시간이 지나면 사라지기 때문에 분산락 알고리즘을 구현할때, 캐시가 영구적으로 남는 이슈에 대한 부담을 덜을 수 있다. 그래도 PID등 여러 부분들을 체크해야 하기 때문에 여전히 복잡하다.</p>

<h3 id="아니-그냥-라이브러리를-쓰십쇼">아니 그냥 라이브러리를 쓰십쇼</h3>

<p>직접 구현하려고 하지 말고, 검증된 라이브러리를 사용하는 것이 좋다.</p>

<ul>
  <li>Django: <code class="language-plaintext highlighter-rouge">cache.lock()</code></li>
  <li>redis-py: <code class="language-plaintext highlighter-rouge">redis.lock()</code></li>
</ul>

<p>코드를 직접 구현하려다 분산락 때문에 또 다른 장애를 만들 수도 있다.</p>

<h1 id="사용시-주의할-점">사용시 주의할 점</h1>

<p>여타 기술들, 좁게는 라이브러리에서 그렇듯, 분산락은 특적 문제에 대한 해결방안을 제공해 주는 도구일 뿐이지 만병통치약은 아니다. 분산락은 멀티 프로세스가 동일한 시스템의 자원을 사용하는 방식이다. 즉 <strong>다른 시스템에 접속은 무조건 필수이기 대문에 둘 중 하나가 이상이 생기면 일관성에 문제가 일어나기 시작한다</strong>. 아래의 항목에 대해 조심해야 한다.</p>

<h2 id="1-프로세스가-shutdown이-되는-경우">1. 프로세스가 Shutdown이 되는 경우</h2>

<p>작업 중 서버가 재시작되면 락 해제가 되지 않으며, 이후 작업들이 교착 상태에 빠진다. try-catch로도 방지 불가하다.</p>

<p><strong>직업 예상 시간을 기준으로 만료 시간을 설정하면 쉽게 해결을 할 수 있다.</strong> 단, 설정된 시간 동안 후속 작업이 지연될 수 있다는 점은 감수해야 한다.</p>

<p>Redlock, 락 감시자 등의 고급 알고리즘도 있으나 구현 복잡도가 높고, 안정성을 확보하려면 추가적인 설계가 필요하다.</p>

<h2 id="2-캐시-시스템이-shutdown-되는-경우">2. 캐시 시스템이 Shutdown 되는 경우</h2>

<p>나도 아직 경험해 보지 않은 이슈이고, 또 어차피 AWS ElastiCache 같은 외부 서비스를 사용하는 경우가 많아 거의 드물지만 <strong>반대로 캐시 시스템이 Shutdown 되는 경우</strong> 가 있다. 실제로 발생하면 1번보다 더 큰 재앙이 될 수 있다.</p>

<ul>
  <li>작업 중단 처리: 캐시 시스템을 사용하지 못한다면, 아예 해당 작업에 대해 요청을 못하게 하는 방법이 있다. HTTP로 치면 500에러를 호출하는 정도가 된다. 가장 쉬운 방법이나, 이는 서비스 장애가 될 수 있다.</li>
  <li>분산락 데이터 백업(replication): 캐시 시스템이 날아가도 기존 서비스는 작동해야 한다. 즉, RDB나 다른 저장소에 분산락이 생길 때 마다 기록을 해 두고, 캐시 시스템에 이상이 발생한다면 분산락 정보를 저장한 저장소가 캐시 시스템을 대신하게 한다. 그나마 서비스 유지가 될 수 있도록 하는 방안이지만, 어떻게 설계하느냐에 다라 구현 방식이 복잡해질 수 있다.</li>
</ul>

<h1 id="마치며">마치며</h1>

<p>분산락은 단순한 기술처럼 보일 수 있지만, 결국 시스템 전반의 신뢰성과 안정성을 좌우하는 요소다. 철저한 예외 처리가 요구되며, 도입 전에 반드시 설계와 운영 비용을 고려해야 한다.</p>

<p>다음 글에서는 Python 기반의 실제 코드를 통해 이 개념을 어떻게 구현할 수 있는지 살펴보겠다.</p>]]></content><author><name></name></author><category term="distributed_lock" /><category term="redis" /><category term="cache" /><category term="distributed-lock" /><category term="software-architecture" /><summary type="html"><![CDATA[배경]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">N+1 문제 실전 사례: DRF ModelSerializer와 List API에서의 쿼리 최적화</title><link href="https://recoma96.github.io/database/2025/04/18/n-plus-1-example-django.html" rel="alternate" type="text/html" title="N+1 문제 실전 사례: DRF ModelSerializer와 List API에서의 쿼리 최적화" /><published>2025-04-18T17:00:00+00:00</published><updated>2025-04-18T17:00:00+00:00</updated><id>https://recoma96.github.io/database/2025/04/18/n-plus-1-example-django</id><content type="html" xml:base="https://recoma96.github.io/database/2025/04/18/n-plus-1-example-django.html"><![CDATA[<h1 id="개요">개요</h1>

<p>한참 전에, <a href="/django/2024/12/12/django-n1-problem.html">Django에서의 N + 1 Problem을 해결하는 방법</a>을 포스팅 한 적이 있었다. 요약하자면, <strong>우리가 사용하는 ORM은 Lazy Loading 기법으로 인해 레코드와 관련된 참조테이블들을 꼭 필요한 때만 가져오기 때문에, 조회된 레코드 갯수 대로 참조 테이블에 쿼리를 날리는 이슈</strong> 정도가 된다. 해결 방법은 <code class="language-plaintext highlighter-rouge">join</code>이나 아니면 참조 테이블을 한번더 <code class="language-plaintext highlighter-rouge">select</code>를 하면 되고, Django에서는 <code class="language-plaintext highlighter-rouge">select_related</code>와 <code class="language-plaintext highlighter-rouge">prefetch_related</code>라는 이름의 함수가 N + 1 Problem을 해결하는 열쇠가 된다.</p>

<p>지금까지는 이 문제를 직접 겪어보진 못했다. 관련 질문을 받은 적은 있었지만, 당시에 명확하게 설명하지 못해 이후에 따로 정리해둔 적이 있을 뿐이다. 그러나 결국 발견했다. 최근 <code class="language-plaintext highlighter-rouge">flask-admin</code> 기반으로 개발된 사내 관리자 페이지를 <code class="language-plaintext highlighter-rouge">django</code>, <code class="language-plaintext highlighter-rouge">react</code>로 리빌딩하는 프로젝트를 진행하면서, 
차량 구독 신청 내역 조회 API를 개발하는 과정에서 N + 1 Problem 문제를 발견했다. ViewSet과 Serlizer를 이용해 API를 개발하고 있는 와중에 뭔가 의심이 들어 쿼리 로그를 봤더니
<strong>조회된 레코드 갯수 대로 참조 테이블을 향해 쿼리를 또 날리는 것이다!</strong> 어떤 이슈였는지 밑의 본론을 통해 알아보자.</p>

<h1 id="본론">본론</h1>

<blockquote>
  <p>사내 기밀 정보가 유출될 수 있기 때문에 실제 사내 DB모델이 아닌 직접 구상한 DB모델로 재현했습니다.</p>
</blockquote>

<h2 id="situation">Situation</h2>

<p>관리자 입장에서 모든 사람들의 일기 리스트를 조회하는 API를 개발해야 한다. 이때 각 일기 데이터 마다, 그 일기를 작성한 유저의 필수정보도 같이 조회해야 한다. 즉 <code class="language-plaintext highlighter-rouge">user</code>와 <code class="language-plaintext highlighter-rouge">short_diary</code>두 개를 한꺼번에 참조해서 리스트를 뽑아야 한다.</p>

<h3 id="모델db-table">모델(DB Table)</h3>

<p>DB 모델 구조는 아래와 같다. 유저 <code class="language-plaintext highlighter-rouge">User</code> 테이블이 있고. 각 유저는 자신만의 여러 개의 짧은 일기 <code class="language-plaintext highlighter-rouge">ShortDiary</code>를 가진다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">User</span><span class="p">(</span><span class="n">AbstractBaseUser</span><span class="p">):</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">db_table</span> <span class="o">=</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span>

    <span class="n">GENDER_CODE</span> <span class="o">=</span> <span class="p">(</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">M</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Male</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">F</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Female</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">)</span>
    <span class="n">email</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">EmailField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">verbose_name</span><span class="o">=</span><span class="sh">"</span><span class="s">이메일</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">nickname</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">CharField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">unique</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">verbose_name</span><span class="o">=</span><span class="sh">"</span><span class="s">닉네임</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">gender</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">CharField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">choices</span><span class="o">=</span><span class="n">GENDER_CODE</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">verbose_name</span><span class="o">=</span><span class="sh">"</span><span class="s">성별</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">is_active</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">BooleanField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">verbose_name</span><span class="o">=</span><span class="sh">"</span><span class="s">활성화 여부</span><span class="sh">"</span><span class="p">)</span>


<span class="k">class</span> <span class="nc">ShortDiary</span><span class="p">(</span><span class="n">models</span><span class="p">.</span><span class="n">Model</span><span class="p">,</span> <span class="n">ModelTimeStampMixin</span><span class="p">):</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">db_table</span> <span class="o">=</span> <span class="sh">"</span><span class="s">short_diary</span><span class="sh">"</span>

    <span class="n">user</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">ForeignKey</span><span class="p">(</span><span class="n">User</span><span class="p">,</span> <span class="n">related_name</span><span class="o">=</span><span class="sh">"</span><span class="s">short_diaries</span><span class="sh">"</span><span class="p">,</span> <span class="n">on_delete</span><span class="o">=</span><span class="n">models</span><span class="p">.</span><span class="n">CASCADE</span><span class="p">)</span>
    <span class="n">title</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">CharField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">256</span><span class="p">)</span>
    <span class="n">context</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">CharField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">max_length</span><span class="o">=</span><span class="mi">1024</span><span class="p">)</span>
    <span class="n">is_deleted</span> <span class="o">=</span> <span class="n">models</span><span class="p">.</span><span class="nc">BooleanField</span><span class="p">(</span><span class="n">null</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

</code></pre></div></div>

<h3 id="serializer">Serializer</h3>

<p>DB 로부터 받은 유저와 일기 데이터를 API를 호출한 클라이언트에게 직렬화 해서 전달하기 위해 <code class="language-plaintext highlighter-rouge">Serializer</code>를 구현해야 한다. <code class="language-plaintext highlighter-rouge">serializers.ModelSerializer</code>를 사용하면 Model과 연동이 
가능하지만, <code class="language-plaintext highlighter-rouge">user_id</code> 같은 왜래키를 통해 참조 테이블까지 직렬화를 하려면 따로 field를 선언해야 한다. 그렇기 때문에 여기서는 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>의 필드에 <code class="language-plaintext highlighter-rouge">UserSerializer</code>를 추가했다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="k">class</span> <span class="nc">UserSerializer</span><span class="p">(</span><span class="n">serializers</span><span class="p">.</span><span class="n">ModelSerializer</span><span class="p">):</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">User</span>
        <span class="n">fields</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">email</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">nickname</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">gender</span><span class="sh">"</span><span class="p">)</span>



<span class="k">class</span> <span class="nc">DiarySerializer</span><span class="p">(</span><span class="n">serializers</span><span class="p">.</span><span class="n">ModelSerializer</span><span class="p">):</span>
    <span class="n">user</span> <span class="o">=</span> <span class="nc">UserSerializer</span><span class="p">(</span><span class="n">read_only</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">ShortDiary</span>
        <span class="n">fields</span> <span class="o">=</span> <span class="sh">"</span><span class="s">__all__</span><span class="sh">"</span>
</code></pre></div></div>

<h3 id="viewset">ViewSet</h3>

<p>딱히 특히 요구사항은 없기 때문에 기본 <code class="language-plaintext highlighter-rouge">ViewSet</code>을 이용해 API를 구현한다. 조회 대상이 <code class="language-plaintext highlighter-rouge">ShortDiary</code>이기 때문에 <code class="language-plaintext highlighter-rouge">queryset</code>을 <code class="language-plaintext highlighter-rouge">ShortDiary.objects.all()</code> 로 잡는다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DiaryViewSet</span><span class="p">(</span>
    <span class="n">viewsets</span><span class="p">.</span><span class="n">ModelViewSet</span><span class="p">,</span>
    <span class="n">mixins</span><span class="p">.</span><span class="n">ListModelMixin</span>
<span class="p">):</span>
    <span class="n">queryset</span> <span class="o">=</span> <span class="n">ShortDiary</span><span class="p">.</span><span class="n">objects</span><span class="p">.</span><span class="nf">all</span><span class="p">()</span>
    <span class="n">serializer_class</span> <span class="o">=</span> <span class="n">UserSerializer</span>
</code></pre></div></div>

<h3 id="db-query-logging">DB Query Logging</h3>

<p>여기까지 세팅을 했다면, 이제 일기 리스트 조회 API는 완성이 되었고, 실제로 정상적으로 잘 돌아간다. 특정한 Warning 조차도 없이 정상적으로 돌아갈 것이다. 그러나 과연 정상적으로 돌아가는 것이 맞을까? Django를 사용하다 보면, ORM코드에 따라 DB Query가 어떻게 생성되는지 궁금할 때가 있을 것이다. 그래서 Django에서는 <code class="language-plaintext highlighter-rouge">settings.py</code>에 아래 코드를 추가하면, ORM으로 인한 데이터를 가져올 때마다 DB Query까지 로깅할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">LOGGING</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">'</span><span class="s">version</span><span class="sh">'</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
    <span class="sh">'</span><span class="s">disable_existing_loggers</span><span class="sh">'</span><span class="p">:</span> <span class="bp">False</span><span class="p">,</span>
    <span class="sh">'</span><span class="s">handlers</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">'</span><span class="s">console</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">'</span><span class="s">class</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">logging.StreamHandler</span><span class="sh">'</span><span class="p">,</span>
            <span class="sh">'</span><span class="s">level</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">DEBUG</span><span class="sh">'</span>
        <span class="p">}</span>
    <span class="p">},</span>
    <span class="sh">'</span><span class="s">loggers</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">'</span><span class="s">django.db.backends</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">'</span><span class="s">handlers</span><span class="sh">'</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">console</span><span class="sh">'</span><span class="p">],</span>
            <span class="sh">'</span><span class="s">level</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">DEBUG</span><span class="sh">'</span><span class="p">,</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span><span class="sh">"</span><span class="s">
</span></code></pre></div></div>

<p>저렿게 하고 API를 호출하면 쿼리 로그는 아래와 같이 출력된다.</p>

<h3 id="n--1-problem">N + 1 Problem</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(0.001) SELECT `short_diary`.`id`, `short_diary`.`user_id`, `short_diary`.`title`, `short_diary`.`context`, `short_diary`.`is_deleted` FROM `short_diary`; args=(); alias=default

(0.001) SELECT `user`.`id`, `user`.`password`, `user`.`last_login`, `user`.`is_superuser`, `user`.`email`, `user`.`nickname`, `user`.`gender`, `user`.`is_active` FROM `user` WHERE `user`.`id` = 23 LIMIT 21; args=(23,); alias=default
(0.001) SELECT `user`.`id`, `user`.`password`, `user`.`last_login`, `user`.`is_superuser`, `user`.`email`, `user`.`nickname`, `user`.`gender`, `user`.`is_active` FROM `user` WHERE `user`.`id` = 23 LIMIT 21; args=(23,); alias=default
</code></pre></div></div>

<p>일단 DB 안에는 1명의 유저가 있고, 이 유저는 2개의 일기를 가지고 있다. 그런데 맨 처음에 일기 <strong>만</strong> 조회 한 다음, 일기 리스트를 순회하면서 <code class="language-plaintext highlighter-rouge">user_id</code>를 추출해서 해당 <code class="language-plaintext highlighter-rouge">id</code>에 해당하는 유저를 조회하기 위해 추가로 Query문을 날리고 있다. 즉, 전체 리스트를 1번 조회하고, 추가로 그 개수(N개) 만큼 조회를 함으로써 <strong>N + 1 Problem</strong> 이 발생한 것이다. 지금이야 데이터가 딱 두개 밖에 없으니 우리 눈에는 문제가 없는 것 처럼 보이겠지만, 데이터 개수가 100개가 넘어간다고 생각해 보자. <em>왜 사용하는 사람이 별로 없는데 API 응답 시간이 오래 걸리지?</em> 하는 생각이 들 것이다.</p>

<h2 id="drf-내부-코드-분석">DRF 내부 코드 분석</h2>

<blockquote>
  <p>왜 ModelSerializer에서 Lazy Loading이 발생하지?</p>
</blockquote>

<p>이전 <a href="/django/2024/12/12/django-n1-problem.html">N + 1 Problem 포스트</a> 에서 설명했듯. <strong>Lazy Loading 기법에 의해 꼭 필요할 때만 데이터를 추출한다.</strong> 가 원인이다. 그러면 꼬리질문이 들어갈 것이다. <strong>왜 Lazy Loading이 발생하는 거지?</strong> 엔지니어라면 왜 이 문제가 언제 어디에서 발생했는지 정도는 정확히 파악할 필요가 있다.</p>

<h3 id="diaryviewset">DiaryViewSet</h3>

<p>일단 아래의 ViewSet을 다시 보자</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DiaryViewSet</span><span class="p">(</span>
    <span class="n">viewsets</span><span class="p">.</span><span class="n">ModelViewSet</span><span class="p">,</span>
    <span class="n">mixins</span><span class="p">.</span><span class="n">ListModelMixin</span>
<span class="p">):</span>
    <span class="n">queryset</span> <span class="o">=</span> <span class="n">ShortDiary</span><span class="p">.</span><span class="n">objects</span><span class="p">.</span><span class="nf">all</span><span class="p">()</span>
    <span class="n">serializer_class</span> <span class="o">=</span> <span class="n">UserSerializer</span>


    <span class="k">def</span> <span class="nf">list</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">request</span><span class="p">,</span> <span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="c1"># mixins.ListModelMixin 의 list 함수를 그대로 가져온 것
</span>        <span class="n">queryset</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">filter_queryset</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">get_queryset</span><span class="p">())</span>

        <span class="n">page</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">paginate_queryset</span><span class="p">(</span><span class="n">queryset</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">page</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">serializer</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">get_serializer</span><span class="p">(</span><span class="n">page</span><span class="p">,</span> <span class="n">many</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
            <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">get_paginated_response</span><span class="p">(</span><span class="n">serializer</span><span class="p">.</span><span class="n">data</span><span class="p">)</span>

        <span class="n">serializer</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">get_serializer</span><span class="p">(</span><span class="n">queryset</span><span class="p">,</span> <span class="n">many</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="k">return</span> <span class="nc">Response</span><span class="p">(</span><span class="n">serializer</span><span class="p">.</span><span class="n">data</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">list()</code> 함수에서 마지막에 응답값을 리턴할 때 <code class="language-plaintext highlighter-rouge">Serializer</code>의 <code class="language-plaintext highlighter-rouge">data</code> 프로퍼티를 호출하는 것을 볼 수 있다.</p>

<h3 id="property-def-data">@property def data</h3>

<p>다음은 <code class="language-plaintext highlighter-rouge">Serializer</code>의 <code class="language-plaintext highlighter-rouge">data</code> 프로퍼티 코드의 일부이다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@property</span>
<span class="k">def</span> <span class="nf">data</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="p">...</span> <span class="n">이하</span> <span class="n">생략</span> <span class="bp">...</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="nf">hasattr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="sh">'</span><span class="s">_data</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">instance</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span> <span class="ow">and</span> <span class="ow">not</span> <span class="nf">getattr</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="sh">'</span><span class="s">_errors</span><span class="sh">'</span><span class="p">,</span> <span class="bp">None</span><span class="p">):</span>
            <span class="n">self</span><span class="p">.</span><span class="n">_data</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">to_representation</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">instance</span><span class="p">)</span>
    <span class="p">...</span> <span class="n">이하</span> <span class="n">생략</span> <span class="bp">...</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Serializer</code>는 DB 인스턴스를 받아 <code class="language-plaintext highlighter-rouge">self.instance</code>에 저장하고 <code class="language-plaintext highlighter-rouge">to_representation</code>을 호출해 직렬화 과정을 수행한다.</p>

<h3 id="listserializerto_representation">ListSerializer.to_representation</h3>

<p>다음은 <code class="language-plaintext highlighter-rouge">ListSerializer.to_representation</code> 함수의 일부다. <code class="language-plaintext highlighter-rouge">to_representation</code>은 DB 인스턴스를 파라미터로 받고 <code class="language-plaintext highlighter-rouge">dict</code> 형태로 직렬화를 하는 함수로, 보통 Serializer에서 직렬화 해주는 데이터 말고도 다른 추가적인 데이터도 같이 직렬화 해야 할때 해당 함수를 오버라이딩을 하는 경우가 종종 있다.</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">ModelSerializer</code>가 아닌 <code class="language-plaintext highlighter-rouge">ListSerializer</code>를 보는 이유는, <code class="language-plaintext highlighter-rouge">list()</code>에서 <code class="language-plaintext highlighter-rouge">serializer</code>를 가져올 때 <code class="language-plaintext highlighter-rouge">self.get_serializer(page, many=True)</code> 처럼 파라미터에 <code class="language-plaintext highlighter-rouge">many=True</code>로 설정하면. <code class="language-plaintext highlighter-rouge">ListSerializer</code>로 매핑이 되기 때문이다. 결국 최상단에 사용되는 Serializer는 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>가 아닌 <code class="language-plaintext highlighter-rouge">ListSerializer</code>이고 <code class="language-plaintext highlighter-rouge">ListSerializer</code>의 <code class="language-plaintext highlighter-rouge">child</code>는 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>가 된다. 이 점을 꼭 기억해야 한다.</p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ListSerializer</span><span class="p">(</span><span class="n">BaseSerializer</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">to_representation</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">data</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        List of object instances -&gt; List of dicts of primitive datatypes.
        </span><span class="sh">"""</span>
        <span class="c1"># Dealing with nested relationships, data can be a Manager,
</span>        <span class="c1"># so, first get a queryset from the Manager if needed
</span>        <span class="n">iterable</span> <span class="o">=</span> <span class="n">data</span><span class="p">.</span><span class="nf">all</span><span class="p">()</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">models</span><span class="p">.</span><span class="n">manager</span><span class="p">.</span><span class="n">BaseManager</span><span class="p">)</span> <span class="k">else</span> <span class="n">data</span>

        <span class="k">return</span> <span class="p">[</span>
            <span class="n">self</span><span class="p">.</span><span class="n">child</span><span class="p">.</span><span class="nf">to_representation</span><span class="p">(</span><span class="n">item</span><span class="p">)</span> <span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">iterable</span>
        <span class="p">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">return</code> 부에 <code class="language-plaintext highlighter-rouge">iterable</code>, 즉 일기 리스트 DB 쿼리에 대해 순회를 하면서 각 리스트 요소 마다 <code class="language-plaintext highlighter-rouge">self.child.to_representation</code>을 호출하는 것을 볼 수가 있다. <code class="language-plaintext highlighter-rouge">self.child</code>는 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>이고 <code class="language-plaintext highlighter-rouge">item</code>은 <code class="language-plaintext highlighter-rouge">diary</code> 인스턴스가 될 것이다. 그리고 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>는 <code class="language-plaintext highlighter-rouge">UserSerializer</code>를 필드로 가지고 있을 것이고, <code class="language-plaintext highlighter-rouge">DiarySeiralizer.to_representation</code>이 실행될 때마다 <code class="language-plaintext highlighter-rouge">diary.user</code>가 <code class="language-plaintext highlighter-rouge">UserSerializer</code>안에 들어가서 유저 정보를 직렬화 할 것이다. 대강 로직을 구상해 보면 아래와 같다고 보면 된다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">arr</span> <span class="o">=</span> <span class="p">[]</span>

<span class="k">for</span> <span class="n">diary</span> <span class="ow">in</span> <span class="n">diaries</span><span class="p">:</span>
    <span class="n">user_indstance</span><span class="p">:</span> <span class="n">User</span> <span class="o">=</span> <span class="n">diary</span><span class="p">.</span><span class="n">user</span>
    <span class="n">arr</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Serializer</span><span class="p">(</span><span class="n">diary</span><span class="p">,</span> <span class="n">diary</span><span class="p">.</span><span class="n">user</span><span class="p">).</span><span class="n">data</span><span class="p">)</span>
</code></pre></div></div>

<p>일기 리스트를 순회할 때마다 <code class="language-plaintext highlighter-rouge">diary.user</code>를 사용한다. 그런데 이 <code class="language-plaintext highlighter-rouge">diary.user</code>에 대해서 의구심이 가기 시작한다. 아까 설명했듯. Django ORM은 <code class="language-plaintext highlighter-rouge">Lazy Loading</code> 기법으로 인해 꼭 필요할 대만 DB에 쿼리를 던진다고 했다. 그렇기 때문에 <code class="language-plaintext highlighter-rouge">Diary</code>에 대한 실제 DB 쿼리를 날리는 시점은 <code class="language-plaintext highlighter-rouge">to_representation</code>의 <code class="language-plaintext highlighter-rouge">return</code>부분이다. 순회를 하면서 실질적으로 <code class="language-plaintext highlighter-rouge">diary</code>를 꺼내오기 때문이다. <strong>그렇다면 DB 쿼리를 날릴 때 <code class="language-plaintext highlighter-rouge">diary</code> 뿐만 아니라 <code class="language-plaintext highlighter-rouge">user</code> 테이블인 <code class="language-plaintext highlighter-rouge">diary.user</code>도 같이 딸려서 들어왔을까?</strong> 아까 <code class="language-plaintext highlighter-rouge">ViewSet</code>에서 <code class="language-plaintext highlighter-rouge">queryset</code>을 어떻게 정의했더라… 다시 한번 살펴보자.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>queryset = ShortDiary.objects.all()
</code></pre></div></div>

<p>그냥 <code class="language-plaintext highlighter-rouge">ShortDiary</code>만 조회한다. 이 코드 어디에도 <code class="language-plaintext highlighter-rouge">user</code>와 관련된 흔적은 없다. 설령 <code class="language-plaintext highlighter-rouge">ShortDiary</code> Model에 <code class="language-plaintext highlighter-rouge">user</code>과 관련된 <code class="language-plaintext highlighter-rouge">relationship</code>이 선언되었더라도 일단 이 코드에서는 
<code class="language-plaintext highlighter-rouge">user</code> 관련된 코드는 보이지 않는다. <strong>눈씻고 찾아봐도 <code class="language-plaintext highlighter-rouge">user</code> 를 사용하려는 코드가 보이지 않기 때문에 Django는 굳이 불필요하게 user까지 데이터를 가져오는 비효율적인 행위를 하지 않는 것이다.</strong> 왜? 개발자가 그렇게 코드를 짰으니까.</p>

<h2 id="요약">요약</h2>

<p>결국 ORM을 사용, 혹은 <code class="language-plaintext highlighter-rouge">ModelSerializer</code>같은 ORM기능이 포함된 모듈을 사용할 때, <strong><code class="language-plaintext highlighter-rouge">user</code>를 사용한다는 표시를 하지 않았기 때문에</strong> 처음에 <code class="language-plaintext highlighter-rouge">diary</code>를 조회할 때 <code class="language-plaintext highlighter-rouge">user</code>를 같이 조회하지 않았고. 
이는 결국 리스트를 순회하면서 <code class="language-plaintext highlighter-rouge">DiarySerializer</code>에서 각 DB 인스턴스에 대해 직렬화를 수행할 때, <code class="language-plaintext highlighter-rouge">user</code> 관련 정보를 로드하지 않았기 때문에, 반복문이 돌아가는 동안 <code class="language-plaintext highlighter-rouge">user</code> 조회를 반복해서 실행하는 N + 1 Problem이 터진 것이다. 이 문제를 해결하는 근본적인 방법은 <strong><code class="language-plaintext highlighter-rouge">user</code>를 사용한다는 표시를 해주면 된다.</strong></p>

<h2 id="solution">Solution</h2>

<p>해결방법은 간단하고 명료하다. 코드로 Django에게 “나 <code class="language-plaintext highlighter-rouge">user</code>도 같이 쓸거야” 라고 말해주면 된다. 그러니까, <code class="language-plaintext highlighter-rouge">ViewSet</code>에 <strong><code class="language-plaintext highlighter-rouge">queryset</code>에 <code class="language-plaintext highlighter-rouge">user</code>를 미리 로딩 <code class="language-plaintext highlighter-rouge">eager loading</code> 하게 하면 된다. 즉, <code class="language-plaintext highlighter-rouge">select_related</code>를 사용하면 된다.</strong>
이렇게 되면 처음부터 <code class="language-plaintext highlighter-rouge">join</code>을 이용해 <code class="language-plaintext highlighter-rouge">user</code> 정보도 미리 쿼리에 포함되어 있기 때문에 더이상 N + 1 Problem이 발생하지 않게 된다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DiaryViewSet</span><span class="p">(</span>
    <span class="n">viewsets</span><span class="p">.</span><span class="n">ModelViewSet</span><span class="p">,</span>
    <span class="n">mixins</span><span class="p">.</span><span class="n">ListModelMixin</span>
<span class="p">):</span>
    <span class="n">queryset</span> <span class="o">=</span> <span class="n">ShortDiary</span><span class="p">.</span><span class="n">objects</span><span class="p">.</span><span class="nf">select_related</span><span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">).</span><span class="nf">all</span><span class="p">()</span>
    <span class="n">serializer_class</span> <span class="o">=</span> <span class="n">DiarySerializer</span>
</code></pre></div></div>

<h1 id="끝-회고">끝 (회고)</h1>

<p>이 경험을 통해, 코드 한줄에도 성능 이슈가 숨어 있을 수 있다는 걸 깨닫게 되었다. 앞으로는 코드 한줄한줄 작성할 때마다 이게 어떤 영향을 끼치는지 한번은 확인하는게 좋을 것 같다. 특히 코드 몇 자로 API가 바로 구현이 되는 <code class="language-plaintext highlighter-rouge">Django</code>에서는 더 신경을 써야 할 것 같다. 편함에 속아 내부적인 상황을 파악하지 않으면 안되니까 말이다.</p>]]></content><author><name></name></author><category term="Database" /><category term="django" /><category term="database" /><category term="orm" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Python Double List Comprehension으로 2차원 배열을 1차원으로 만들기</title><link href="https://recoma96.github.io/python/2025/04/09/python-double-list-comprehension.html" rel="alternate" type="text/html" title="Python Double List Comprehension으로 2차원 배열을 1차원으로 만들기" /><published>2025-04-09T07:00:00+00:00</published><updated>2025-04-09T07:00:00+00:00</updated><id>https://recoma96.github.io/python/2025/04/09/python-double-list-comprehension</id><content type="html" xml:base="https://recoma96.github.io/python/2025/04/09/python-double-list-comprehension.html"><![CDATA[<h1 id="list-comprehension">List Comprehension</h1>

<p>Python의 List Comprehension은 어떤 배열을 다른 배열로 매핑할 때 사용되는 트릭으로 일반적인 <code class="language-plaintext highlighter-rouge">for</code> 문 보다 더 코드를 깔끔하게 작성할 수 있다. 예를 들어 
<code class="language-plaintext highlighter-rouge">[1,2,3,4]</code> 배열의 요소에 2를 곱한 <code class="language-plaintext highlighter-rouge">[2,4,6,8]</code> 로 표현하고자 할때</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">src</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">]</span>

<span class="n">dst</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">element</span> <span class="ow">in</span> <span class="n">src</span><span class="p">:</span>
    <span class="n">dst</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">element</span> <span class="o">*</span> <span class="mi">2</span><span class="p">)</span>
</code></pre></div></div>

<p>이렇게 작성할 코드를 List Comprehension을 사용하면</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">src</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">]</span>

<span class="n">dst</span> <span class="o">=</span> <span class="p">[</span><span class="n">element</span> <span class="o">*</span> <span class="mi">2</span> <span class="k">for</span> <span class="n">element</span> <span class="ow">in</span> <span class="n">src</span><span class="p">]</span>
</code></pre></div></div>

<p>이렇게 보기좋게 작성할 수 있다. 그렇다 보니 복잡한 로직이 아닌 이상 어지간하면 리스트 컴프리헨션을 쓰는 경우가 많으며. <code class="language-plaintext highlighter-rouge">map</code>함수가 버젓이 있음에도 불구하고 이쪽을 더 많이 쓴다.</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">map</code>함수를 사용할 경우 아래와 같다. 일반 for문 보다는 코드가 줄긴 했지만, 리스트 컴프리헨션보다는 코드 가시성이 조금 아쉽다.</p>
  <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">src</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">]</span>
<span class="n">dst</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">map</span><span class="p">(</span><span class="k">lambda</span> <span class="n">e</span><span class="p">:</span> <span class="n">e</span> <span class="o">*</span> <span class="mi">2</span><span class="p">,</span> <span class="n">src</span><span class="p">))</span>
</code></pre></div>  </div>
</blockquote>

<h1 id="이중-list-comprehension">이중 List Comprehension</h1>

<p>그런데 최근 문득 궁금증이 생긴다. <em>“이중 리스트 컴프리헨션도 작성할 수 있을까?”</em></p>

<p>예를 들어 2차원 배열 <code class="language-plaintext highlighter-rouge">[[1,2,3], [4,5,6]]</code>를 <code class="language-plaintext highlighter-rouge">[1,2,3,4,5,6]</code> 1차원 배열로 바꾼다고 가정해 보자. 리스트 컴프리헨션을 쓰지 않았다면 코드는 아래와 같다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="n">arr</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">],</span>
    <span class="p">[</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">6</span><span class="p">],</span>
<span class="p">]</span>

<span class="n">dst</span> <span class="o">=</span> <span class="p">[]</span>

<span class="k">for</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">arr</span><span class="p">:</span>
    <span class="k">for</span> <span class="n">element</span> <span class="ow">in</span> <span class="n">dst</span><span class="p">:</span>
        <span class="n">dst</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">element</span> <span class="o">*</span> <span class="mi">2</span><span class="p">)</span>

</code></pre></div></div>

<p>이걸 이중 List Comprehension으로 표현하면 다음과 같다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">arr</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">],</span>
    <span class="p">[</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">6</span><span class="p">],</span>
<span class="p">]</span>

<span class="n">dst</span> <span class="o">=</span> <span class="p">[</span><span class="n">element</span> <span class="o">*</span> <span class="mi">2</span> <span class="k">for</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">arr</span> <span class="k">for</span> <span class="n">element</span> <span class="ow">in</span> <span class="n">row</span><span class="p">]</span>
</code></pre></div></div>

<p>저 코드가 뭔지 모를 수 있는데 이중 for 문과 비교하면 아래와 같다.</p>

<p><img src="/assets/img/20250410/1.jpg" alt="" /></p>

<p>for문 depth는 List Comprehension에서 <strong>왼쪽부터 오른쪽으로 이동한다.</strong> 단 최후에 출력될 데이터 <code class="language-plaintext highlighter-rouge">element</code> 는 depth에 상관없이 <strong>무조건 왼쪽으로</strong> 간다. element가 왼쪽에 배치되기 때문에 for문도 오른쪽부터 왼쪽으로 진행할 거라 생각하는데 절대 아니다. 그래서 이 부분은 처음 접할 때 헷갈리는 경우가 많다.</p>

<h2 id="대안-itertoolschainfrom_iterable-사용하기">대안: itertools.chain.from_iterable 사용하기</h2>

<p>개인적인 생각으로는 솔직히 이중 컴프리헨션은 코드 순서부터 헷갈리기 때문에 <strong>혼란만 야기할 거라고 생각한다.</strong> 그렇다 보니 이중 리스트 컴프리헨션의 대안이 따로 있는데 그게 바로 <code class="language-plaintext highlighter-rouge">itertools.chain.from_iterable</code> 이다. Python 내장 패키지 중 하나인 <code class="language-plaintext highlighter-rouge">itertools</code>는 순혈, 조합 등 프로그래밍을 하는 데 있어 여러 알고리즘을 제공하는데 <code class="language-plaintext highlighter-rouge">itertools.chain.from_iterable</code>는 2차원 배열을 1차원 배열로 펴는데 사용된다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">itertools</span> <span class="kn">import</span> <span class="n">chain</span>


<span class="n">arr</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">],</span>
    <span class="p">[</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">6</span><span class="p">],</span>
<span class="p">]</span>

<span class="n">dst</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">chain</span><span class="p">.</span><span class="nf">from_iterable</span><span class="p">(</span><span class="n">arr</span><span class="p">))</span>
</code></pre></div></div>

<h2 id="용도">용도</h2>

<p>뭔가 이딴건 사용하지 않을 것 같지만. 간혹 있는 모양이다. 나의 경우 django 기반의 관리자 페이지 API 서버를 개발할 때. 복합키 이슈로 조회 <code class="language-plaintext highlighter-rouge">select</code>를를 Raw Query를 사용하는 경우가 있었다. 정확히는 복합키 여러개를 가지고 조회하는 쿼리였다.s <em>(django model은 복합키를 지원하지 않는다.)</em></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>select ... 생략 ... from table where (pk1, pk2) in ((%s, %s), (%s, %s), ...)
</code></pre></div></div>

<p>그리고 내가 가지고 있는 배열은 복합키로 이루어진 2중 배열이었다</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">],</span>
    <span class="p">[</span><span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">],</span>
<span class="p">]</span>
</code></pre></div></div>

<p>즉 저 복합키 2중 배열을 쿼리문에 바인딩을 해야 하는데 바인딩을 하려면 1차원 배열이 필요했으므로 2차원 배열을 1차원 배열로 변형해야 하는 일이 생겼다. 그래서 이중 List Comprehension을 사용하여 해당 문제를 해결했다. <em>(나중가서는 <code class="language-plaintext highlighter-rouge">itertools.chain.from_iterable</code>을 사용했다.)</em></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">],</span>
    <span class="p">[</span><span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">],</span>
<span class="p">]</span>

<span class="n">placeholders</span> <span class="o">=</span> <span class="sh">"</span><span class="s">, </span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">([</span><span class="sh">"</span><span class="s">(%s, %s)</span><span class="sh">"</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">))</span>
<span class="n">flat_values</span> <span class="o">=</span> <span class="p">[</span><span class="n">element</span> <span class="k">for</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">data</span> <span class="k">for</span> <span class="n">element</span> <span class="ow">in</span> <span class="n">row</span><span class="p">]</span>

<span class="n">sql</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">select * from table where (pk1, pk2) in (</span><span class="si">{</span><span class="n">placeholders</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span>

<span class="k">with</span> <span class="n">connections</span><span class="p">[</span><span class="sh">"</span><span class="s">main-database</span><span class="sh">"</span><span class="p">].</span><span class="nf">cursor</span><span class="p">()</span> <span class="k">as</span> <span class="n">cursor</span><span class="p">:</span>
    <span class="n">cursor</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span><span class="n">sql</span><span class="p">,</span> <span class="n">flat_values</span><span class="p">)</span>
    <span class="n">results</span> <span class="o">=</span> <span class="n">cursor</span><span class="p">.</span><span class="nf">fetchall</span><span class="p">()</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="Python" /><category term="python" /><summary type="html"><![CDATA[List Comprehension]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">AWS Lambda Layer 활용해 보기 (Python 기준)</title><link href="https://recoma96.github.io/aws/2025/03/05/aws-lambda-layer.html" rel="alternate" type="text/html" title="AWS Lambda Layer 활용해 보기 (Python 기준)" /><published>2025-03-05T11:30:00+00:00</published><updated>2025-03-05T11:30:00+00:00</updated><id>https://recoma96.github.io/aws/2025/03/05/aws-lambda-layer</id><content type="html" xml:base="https://recoma96.github.io/aws/2025/03/05/aws-lambda-layer.html"><![CDATA[<h1 id="개요">개요</h1>

<p>Lambda Function은 주로 Serverless API를 구현할 때 사용된다. 말 그대로 Serverless이기 때문에 API 작동에 필요한 모든 인프라 세팅은 AWS가 담당하고 개발자는 그저 코딩만 하면 구조다.
그래서 이러한 편의성 덕분에 단순 API부터 배치 프로세스 까지 다양한 용도로 활발하게 사용이 되고 있다.
그런데 하나 단점이 있다면, 외부 모듈(라이브러리) 사용을 위한 설치인데, 일반적인 개발환경에서는 <code class="language-plaintext highlighter-rouge">pip</code> 같은 패키징 관련 앱을 통해 라이브러리 설치가 가능하고, 앱 빌드에 사용되는 <code class="language-plaintext highlighter-rouge">docker</code>, <code class="language-plaintext highlighter-rouge">github action</code> 등에서도
배포 프로세스에 개발자가 라이브러리를 설치하는 코드를 추가할 수 있지만, <strong>Lambda Console에서는 직접 라이브러리 설치가 불가능하다.</strong> 대신 <strong><code class="language-plaintext highlighter-rouge">Lambda Layer</code> 를 사용해야 한다.</strong></p>

<h1 id="aws-lambda-layer">AWS Lambda Layer</h1>

<p><code class="language-plaintext highlighter-rouge">Lambda Layer</code> 는 AWS Lambda에서 사용하는 모듈 단위로, pymysql이나, requests 같은 외부 모듈 또는 공통으로 사용하는 모듈들은 Lambda에서 Layer 단위로 되어 있다.
이러한 모듈들을 사용하기 위해서는 Lambda Function에 Layer를 추가해야 한다.</p>

<h2 id="외부-라이브러리-추가하기">외부 라이브러리 추가하기</h2>

<p>http 요청 모듈은 requests 라이브러리를 설치한다고 가정한다.</p>

<ol>
  <li>
    <p>람다 콘솔에서 스크롤을 쭉 내려 Layers 항목을 확인하고 “Add Layer” 버튼을 누른다.
<img src="/assets/img/20250305/1.png" alt="" /></p>
  </li>
  <li>
    <p>AWS Layers가 아닌 Specity an ARN을 선택한다. AWS Layers에서는 requests 관련 모듈이 없기 때문이다.
<img src="/assets/img/20250305/2.png" alt="" /></p>
  </li>
  <li>
    <p>그러나 requests 모듈이 정확히 어떤 ARN을 가지는지 바로 알 수가 없다. 하지만 파이썬의 경우, 다행히도 파이썬 버전에 따라 사용 가능한 라이브러리의 ARN을 나열한 사이트가 있기 때문에 거기서 찾으면 된다.</p>
    <ul>
      <li><a href="https://github.com/keithrozario/Klayers/tree/master/deployments/python3.12">Python3.12 에서 사용가능한 ARN 리스트</a></li>
      <li><a href="https://github.com/keithrozario/Klayers?tab=readme-ov-file">Klayer Github Repository</a></li>
    </ul>

    <blockquote>
      <p>아무리 찾아봐도 없다면 직접 대상 모듈을 다운로드받고 압축한 다음에 AWS에 업로드해서 사용하는 방법도 있다.</p>
    </blockquote>
  </li>
</ol>

<p><img src="/assets/img/20250305/3.png" alt="" /></p>

<ol>
  <li>
    <p>ARN을 입력하고 “verify” 버튼을 클릭해 이 ARN이 제대로 되어있는지 검증한다. 아래와 같이 패키지 정보가 보이면 검증에 통과되었다는 의미로 “Add” 버튼을 클릭해 레이어를 추가하면 된다.
<img src="/assets/img/20250305/4.png" alt="" /></p>
  </li>
  <li>
    <p>다시 람다 콘솔로 돌아가서 해당 라이브러리를 임포트해 사용을 해 본다. 정상적으로 임포트가 되었다면 레이어 추가에 성공한 것이다.</p>
  </li>
</ol>

<h2 id="직접-라이브러리를-구현해서-추가하기">직접 라이브러리를 구현해서 추가하기</h2>

<p>여러 Lambda를 구현하다 보면, 공통로직을 복붙하는 일이 한번씩은 생길 것이다 <em>(예를 들어 데이터베이스 접속 로직 이라던가)</em>. 일반적인 개발환경이라면 공통모듈 전용 파일을 만들고 그 안에 API들을 구현해 재활용할 수 있지만. Lambda들 사이에는 안타깝께도 그렇게 할 수가 없다. 이러한 문제를 해결하기 위해 Layer가 사용되기도 한다.</p>

<ol>
  <li>모듈을 구현한다. 이때 디렉토리는 아래와 같아야 한다.
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/package-name
`-/python
     `-/package-name
         `-__init__.py
     `-requirements.txt
</code></pre></div>    </div>
  </li>
  <li>zip 파일로 압축을 한다. 이때 압축 위치는 최상위 디렉토리가 아닌 <strong>python 디렉토리가 되어야 한다.</strong></li>
  <li>
    <p>AWS 콘솔에서 Lambda &gt; Layers로 들어간다. 여기서 “Create Layer” 버튼을 클릭한다.
<img src="/assets/img/20250305/5.png" alt="" /></p>
  </li>
  <li>
    <p>여기서 패키지 이름과 압축파일 python.zip을 올려서 레이어를 생성한다.
<img src="/assets/img/20250305/6.png" alt="" /></p>
  </li>
  <li>
    <p>생성에 성공하면 아래와 같은 화면이 표시되는데 여기서 Version은 말 그대로 해당 모듈의 버전으로, 다시 추가로 코드를 작성해서 업로드를 하면 이 Version이 올라간다.
<img src="/assets/img/20250305/7.png" alt="" /></p>
  </li>
  <li>
    <p>해당 레이어를 사용할 람다 콘솔로 돌아가서 레이어를 추가한다. “Custom Layers” 항목을 클릭하고 아까 새로 만들었던 레이어의 이름과 버전을 선택한다.
<img src="/assets/img/20250305/8.png" alt="" /></p>
  </li>
  <li>정상적으로 적용되었는지 임포트에서 사용해본다.</li>
</ol>

<h1 id="활용">활용</h1>

<h3 id="lamda에서-aws-secrets-manager의-환경변수-가져오기">Lamda에서 AWS Secrets Manager의 환경변수 가져오기</h3>

<p>Lambda상의 환경변수는 Configuration &gt; Environment variables탭에서 직접 추가가 가능하다. 그러나 AWS 기반 인프라를 구축하다 보면 데이터베이스 접속 계정같은 공통 환경변수들을 Secrets Manager에 보관하는 경우가 있기 때문에 그쪽으로 접근해야 할 때가 있다. 그러나 boto3로 빈번하게 접근하면 이게 다 비용으로 나기기 때문에 일부는 boto3 대신 아래 이름의 확장팩을 사용하는 경우가 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AWS-Parameters-and-Secrets-Lambda-Extension
</code></pre></div></div>

<p>해당 layer는 환경변수를 가져온 다음 람다 내부에 캐싱을 해서 다음에 다시 가져올때 로컬 내 캐싱된 데이터만 가져오게 함으로써 접근 비용을 절약할 수 있다. 단, boto3 모듈을 사용하는 게 아니라 HTTP통신을 해야 한다. <a href="https://docs.aws.amazon.com/secretsmanager/latest/userguide/retrieving-secrets_lambda.html">사용법 참고</a></p>]]></content><author><name></name></author><category term="AWS" /><category term="aws" /><category term="lambda" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">WARNING: Found orphan containers (***) for this project.</title><link href="https://recoma96.github.io/docker/2025/02/13/docker-orphans-issue.html" rel="alternate" type="text/html" title="WARNING: Found orphan containers (***) for this project." /><published>2025-02-13T11:30:00+00:00</published><updated>2025-02-13T11:30:00+00:00</updated><id>https://recoma96.github.io/docker/2025/02/13/docker-orphans-issue</id><content type="html" xml:base="https://recoma96.github.io/docker/2025/02/13/docker-orphans-issue.html"><![CDATA[<p>내가 사내에서 운영하고 있는 서버의 Docker Container구조는 크게 “backend server”, “celery”, “celerybeat”가 있다.
그리고 이 동일한 서버가 두대가 있고 그 위에 ALB가 배치되어 있다. 그렇다면, 결국 celerybeat가 두개 돌아간다는 뜻이 되는데, 
이렇게 되면 완전 동일한 batch process가 두번 중복되어 작동한다는 뜻이 된다. 이는 즉 리소스 낭비 부터 시작해서 서비스 이슈 까지 
발생할 가능성이 있기 때문에 서버 두개 중 하나만 “celerybeat”를 빌드를 해야 한다. (<em>가장 확실한 방법은 기존 서버 두대에서 Celerybeat와 Celery를 제외시키고 
서버를 하나 더 대여해서 그곳에 빌드를 하는 것인데. 비용 이슈도 있고, 오늘 포스팅은 그게 주제가 아니라서 패스</em>)</p>

<p><br /></p>

<p>그래서 서버에 직접 접속을 해서 <code class="language-plaintext highlighter-rouge">docker-compose</code> 파일에서 container 목록 중에 celerybeat를 제거했다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3'</span>
<span class="na">services</span><span class="pi">:</span>
  <span class="na">app</span><span class="pi">:</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">backend</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
  <span class="na">worker</span><span class="pi">:</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">worker</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
  <span class="na">beat</span><span class="pi">:</span> <span class="s">&lt;- 얘 삭제</span>
    <span class="s">container_name</span><span class="err">:</span> <span class="s">beat</span>
    <span class="s">command</span><span class="err">:</span> <span class="s">sh run_celerybeat.sh</span>
    <span class="s">restart</span><span class="err">:</span> <span class="s">always</span>
</code></pre></div></div>

<p>그리고 빌드를 하기 위해 아래와 같은 명령어를 입력했다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>docker-compose <span class="nt">-f</span> docker-compose.yml up <span class="nt">-d</span>
</code></pre></div></div>

<p>그랬더니 아래와 같은 경고문과 함께 <strong>celery beat 컨테이너가 소멸되지 않았다.</strong></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>WARNING: Found orphan containers <span class="o">(</span>beat<span class="o">)</span> <span class="k">for </span>this project. If you removed or renamed this service <span class="k">in </span>your compose file, you can run this <span class="nb">command </span>with the <span class="nt">--remove-orphans</span> flag to clean it up.
</code></pre></div></div>

<p>“orphans”는 한국어로 번역하면 “고아” 라는 뜻이다. 즉, 고아 상태의 컨테이너를 발견했다는 것인데. <strong>docker compose 파일에는 없는 컨테이너가 OS상에서 발견되었다는 뜻이 된다.</strong></p>

<p><br /></p>

<p>당연히 다음 빌드시 대상 컨테이너를 지워야 하는데 방법은 간단하다. 아까 빌드 명령어에 <code class="language-plaintext highlighter-rouge">--remove-orphans</code> 플래그를 추가하면 된다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>docker-compose <span class="nt">-f</span> docker-compose.yml up <span class="nt">-d</span> <span class="nt">--remove-orphans</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="Docker" /><category term="docker" /><category term="issue" /><summary type="html"><![CDATA[내가 사내에서 운영하고 있는 서버의 Docker Container구조는 크게 “backend server”, “celery”, “celerybeat”가 있다. 그리고 이 동일한 서버가 두대가 있고 그 위에 ALB가 배치되어 있다. 그렇다면, 결국 celerybeat가 두개 돌아간다는 뜻이 되는데, 이렇게 되면 완전 동일한 batch process가 두번 중복되어 작동한다는 뜻이 된다. 이는 즉 리소스 낭비 부터 시작해서 서비스 이슈 까지 발생할 가능성이 있기 때문에 서버 두개 중 하나만 “celerybeat”를 빌드를 해야 한다. (가장 확실한 방법은 기존 서버 두대에서 Celerybeat와 Celery를 제외시키고 서버를 하나 더 대여해서 그곳에 빌드를 하는 것인데. 비용 이슈도 있고, 오늘 포스팅은 그게 주제가 아니라서 패스)]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Factory Boy를 이용해 테스트 데이터를 깔끔하게 만들어 보자</title><link href="https://recoma96.github.io/testing/2025/01/22/factory-boy.html" rel="alternate" type="text/html" title="Factory Boy를 이용해 테스트 데이터를 깔끔하게 만들어 보자" /><published>2025-01-22T09:30:00+00:00</published><updated>2025-01-22T09:30:00+00:00</updated><id>https://recoma96.github.io/testing/2025/01/22/factory-boy</id><content type="html" xml:base="https://recoma96.github.io/testing/2025/01/22/factory-boy.html"><![CDATA[<h1 id="factory-boy">Factory Boy</h1>

<p>테스트코드를 작성하다 보면, 테스트를 시작하기 전에 데이터베이스에 테스트용 데이터를 집어넣는 과정을 거친다. 보통은 ORM으로 데이터들을 생성하기 때문에 가끔 이런 코드를 볼 수가 있다. <em>(Django 기준)</em></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">setUp</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">self</span><span class="p">.</span><span class="n">user</span> <span class="o">=</span> <span class="n">User</span><span class="p">.</span><span class="n">objects</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">readable_id</span><span class="o">=</span><span class="sh">"</span><span class="s">G1234512345</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">customer_key</span><span class="o">=</span><span class="sh">"</span><span class="s">6d1fb2d032214636900ec93b94674d74</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">userName</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">email</span><span class="o">=</span><span class="sh">"</span><span class="s">email@gmail.com</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">gender</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
        <span class="n">phone_number</span><span class="o">=</span><span class="sh">"</span><span class="s">01012341234</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">birth_date</span><span class="o">=</span><span class="sh">"</span><span class="s">19990101</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">user_sns_id</span><span class="o">=</span><span class="sh">"</span><span class="s">user_sns_id</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">name</code>이나 <code class="language-plaintext highlighter-rouge">email</code>같은 테스트 목적이 있는 데이터들을 제외한 나머지 데이터들은 그냥 임의로 적어 놓았음을 알 수 있다. 즉, 테스트에 필요하지 않아 랜덤으로 돌려도 되는 값들을 일일이 리터럴하게 집어넣은 것이다. 이런 식으로 진행하게 되면, 쓸떼없는 코드가 많아져 전체적으로 코드가 더럽게 보일게 뻔하다. 이를 해결하기 위한 라이브러리가 바로 <strong>factory_boy</strong> 이다.</p>

<p><strong>factory_boy</strong>는 테스트시 필요한 값들을 랜덤하게 생성하는 역할을 한다. 그런데 그냥 아무 랜덤값을 주는 게 아니라, <strong>테마에 따라 다른 형태의 값을 주는데</strong>, 예를 들어 임의의 이름이 필요하다 하면 Mario나 Luigi 같은 랜덤으로 돌린  이름을 주고, 주소를 요청하면 주소와 관련되게 임의값을 준다.</p>

<h1 id="설치">설치</h1>

<p>factory_boy 라는 라이브러리를 설치하는 것이기 때문에 방법은 간단하다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>pip <span class="nb">install </span>factory_boy
</code></pre></div></div>

<h1 id="사용법">사용법</h1>

<h2 id="일반-사용법---faker-객체-활용하기">일반 사용법 -&gt; Faker 객체 활용하기</h2>

<p>일반적인 사용법이라 함은 그냥 단순히 랜덤값 하나를 불러오는 것을 의미한다. 이 사용법은 매우 간단하다. 몇줄만 추가하면 되기 때문이다.</p>

<p><code class="language-plaintext highlighter-rouge">faker</code>를 import 하고 <code class="language-plaintext highlighter-rouge">Faker</code> 객체를 하나 만든다. 그 다음에 그 객체에서 내가 원하는 랜덤값 이름의 함수를 사용하면 된다. 예를 들어 내가 이름과 관련된 랜덤값을 원한다면, <code class="language-plaintext highlighter-rouge">.name()</code> 또는  <code class="language-plaintext highlighter-rouge">.user_name()</code>을 사용하면 된다.</p>

<blockquote>
  <p>단, <code class="language-plaintext highlighter-rouge">name()</code>과 <code class="language-plaintext highlighter-rouge">user_name()</code>의 차이점이라면, <code class="language-plaintext highlighter-rouge">name()</code>은 실제 사람의 이름으로 성과 이름이 분리되서 나오고 <code class="language-plaintext highlighter-rouge">user_name()</code>은 게임에서 나오는 게임유저 이름 정도만 생각하면 된다.</p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">faker</span> <span class="kn">import</span> <span class="n">Faker</span>

<span class="n">fake</span> <span class="o">=</span> <span class="nc">Faker</span><span class="p">()</span>

<span class="n">fake</span><span class="p">.</span><span class="nf">name</span><span class="p">()</span> <span class="c1"># Shawn Miller
</span><span class="n">fake</span><span class="p">.</span><span class="nf">user_name</span><span class="p">()</span> <span class="c1"># Miller
</span></code></pre></div></div>

<p>이름 말고도 다른 테마로도 가능하다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">fake</span><span class="p">.</span><span class="nf">date_time</span><span class="p">()</span> <span class="c1"># datetime 타입의 랜덤 날짜
</span><span class="n">fake</span><span class="p">.</span><span class="nf">sentence</span><span class="p">()</span> <span class="c1"># 영어 1문장: General apply international possible old fear.
</span></code></pre></div></div>

<p>그렇다면 아까 위에 제시했던 유저 생성 코드를 아래와 같이 변경할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">faker</span> <span class="kn">import</span> <span class="n">Faker</span>

<span class="k">def</span> <span class="nf">setUp</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">fake</span> <span class="o">=</span> <span class="nc">Faker</span><span class="p">()</span>
    <span class="n">self</span><span class="p">.</span><span class="n">user</span> <span class="o">=</span> <span class="n">User</span><span class="p">.</span><span class="n">objects</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">readable_id</span><span class="o">=</span><span class="sa">f</span><span class="sh">"</span><span class="s">G</span><span class="si">{</span><span class="n">fake</span><span class="p">.</span><span class="nf">random_digit</span><span class="p">(</span><span class="mi">10</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">customer_key</span><span class="o">=</span><span class="n">fake</span><span class="p">.</span><span class="nf">uuid4</span><span class="p">(),</span>
        <span class="n">name</span><span class="o">=</span><span class="n">fake</span><span class="p">.</span><span class="nf">user_name</span><span class="p">(),</span>
        <span class="n">email</span><span class="o">=</span><span class="n">fake</span><span class="p">.</span><span class="nf">email</span><span class="p">(),</span>
        <span class="n">gender</span><span class="o">=</span><span class="n">fake</span><span class="p">.</span><span class="nf">random_int</span><span class="p">(</span><span class="nb">min</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="nb">max</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
        <span class="n">phone_number</span><span class="o">=</span><span class="n">fake</span><span class="p">.</span><span class="nf">phone_number</span><span class="p">(),</span>
        <span class="n">birth_date</span><span class="o">=</span><span class="nf">str</span><span class="p">(</span><span class="n">fake</span><span class="p">.</span><span class="nf">ramdon_digit</span><span class="p">(</span><span class="mi">19900000</span><span class="p">,</span> <span class="mi">20241231</span><span class="p">)),</span>
        <span class="n">user_sns_id</span><span class="o">=</span><span class="nf">str</span><span class="p">(</span><span class="n">fake</span><span class="p">.</span><span class="nf">random_digit</span><span class="p">(</span><span class="mi">20</span><span class="p">)),</span>
    <span class="p">)</span>
</code></pre></div></div>
<p>일단 <strong>상수값들이 사라졌다.</strong> 이제 임의 데이터 생성은 factory boy에서 알아서 해주기 때문이다. 하지만… 뭔가 코드가 더러운건 똑같아 보인다. 그리고 테스트 유저 생성을 이 한 테스트 파일 뿐만 아니라 여러 테스트 파일에도 사용이 될 텐데 이렇게 되면 또 똑같은 식의 코드를 작성해야 한다. 이때 factory boy는 <strong>factory.Factory를 사용함을 제안한다.</strong></p>

<h2 id="factoryfactory">factory.Factory</h2>

<p>이전에 서술했던 <code class="language-plaintext highlighter-rouge">fake.Faker</code>가 단순히 랜덤값을 생성시키는 역할을 한 다면, <code class="language-plaintext highlighter-rouge">factory.Factory</code>는 랜덤값들을 생성함은 물론 <strong>테스트용 데이터베이스에 까지 친절하게 데이터를 저장해 주는 역할을 한다.</strong> 예를 들어 테스트용 <code class="language-plaintext highlighter-rouge">User</code>데이터를 데이터베이스 저장한다고 할때, <code class="language-plaintext highlighter-rouge">factory.Factory</code>를 상속한 <code class="language-plaintext highlighter-rouge">UserFactory</code>를 구현한 다음에 <code class="language-plaintext highlighter-rouge">UserFactory.create()</code>만 작성해 주면 알아서 데이터베이스를 저장해 주고, ORM 인스턴스를 내뱉는다. 사용법은 아래와 같다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># factory.py
</span>
<span class="kn">import</span> <span class="n">factory</span>


<span class="k">class</span> <span class="nc">ProfileFactory</span><span class="p">(</span><span class="n">factory</span><span class="p">.</span><span class="n">django</span><span class="p">.</span><span class="n">DjangoModelFactory</span><span class="p">)</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">Profile</span>
    <span class="p">...</span> <span class="n">이하</span> <span class="n">생략</span> <span class="bp">...</span>


<span class="k">class</span> <span class="nc">UserFactory</span><span class="p">(</span><span class="n">factory</span><span class="p">.</span><span class="n">django</span><span class="p">.</span><span class="n">DjangoModelFactory</span><span class="p">):</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">User</span>

    <span class="n">readable_id</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyFunction</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="nf">uuid4</span><span class="p">())[:</span><span class="mi">10</span><span class="p">])</span>
    <span class="n">customer_key</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">Faker</span><span class="p">(</span><span class="sh">"</span><span class="s">uuid4</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">user_sns_id</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyFunction</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="nf">uuid4</span><span class="p">())[:</span><span class="mi">10</span><span class="p">])</span>
    <span class="n">name</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">Faker</span><span class="p">(</span><span class="sh">"</span><span class="s">user_name</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">email</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">Faker</span><span class="p">(</span><span class="sh">"</span><span class="s">email</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">gender</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyFunction</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">([</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">]))</span>
    <span class="n">phone_number</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">Faker</span><span class="p">(</span><span class="sh">"</span><span class="s">phone_number</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">birth_date</span> <span class="o">=</span> <span class="sh">"</span><span class="s">990101</span><span class="sh">"</span>
    <span class="n">is_staff</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyAttribute</span><span class="p">(</span><span class="k">lambda</span> <span class="n">e</span><span class="p">:</span> <span class="n">e</span><span class="p">.</span><span class="n">is_staff</span><span class="p">)</span>
    <span class="n">profile</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">SubFactory</span><span class="p">(</span><span class="n">ProfileFactory</span><span class="p">)</span>
</code></pre></div></div>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># test.py
</span>
<span class="k">class</span> <span class="nc">UserTestCase</span><span class="p">(</span><span class="n">APITestCaese</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">setUp</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">fake</span> <span class="o">=</span> <span class="nc">Faker</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">profile</span> <span class="o">=</span> <span class="n">ProfileFactory</span><span class="p">.</span><span class="nf">create</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">user</span> <span class="o">=</span> <span class="n">UserFactory</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
            <span class="n">is_staff</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
            <span class="n">profile</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">profile</span><span class="p">,</span>
        <span class="p">)</span>
</code></pre></div></div>

<p>위의 <code class="language-plaintext highlighter-rouge">UserFactory</code>를 정의하는 코드에서 여러 가지의 <code class="language-plaintext highlighter-rouge">factory</code>쪽 함수를 사용하고 있는데 설명하자면 아래와 같다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">name</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">Faker</span><span class="p">(</span><span class="sh">"</span><span class="s">user_name</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">factory.Faker</code>는 가장 기본적으로 쓰이는 함수로, 아까 설명했던 <code class="language-plaintext highlighter-rouge">Faker</code>쪽 함수와 기능이 같다고 보면 된다. 다른 점이 있다면 <code class="language-plaintext highlighter-rouge">Faker</code>에서의 함수 이름이, <code class="language-plaintext highlighter-rouge">factory.Faker</code>에서는 문자열 파라미터로 쓰인다는 것이다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">readable_id</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyFunction</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="nf">uuid4</span><span class="p">())[:</span><span class="mi">10</span><span class="p">])</span>
</code></pre></div></div>

<p>하지만 <code class="language-plaintext highlighter-rouge">factory.Faker</code>로는 랜덤 생성이 불가능한 상황이 있다. 예를 들어 UUID를 랜덤으로 돌리는 것은 <code class="language-plaintext highlighter-rouge">factory.Faker("uuid4")</code>로 해결이 가능하지만, 랜덤으로 돌린 다음, 앞의 10자를 제외한 나머지 문자열을 제거하는 것 까지는 할 수가 없다. 이때는 랜덤 함수를 직접 만들어야 하는데, <code class="language-plaintext highlighter-rouge">factory.LazyFunction</code>이 이를 가능하게 해준다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">is_staff</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">LazyAttribute</span><span class="p">(</span><span class="k">lambda</span> <span class="n">e</span><span class="p">:</span> <span class="n">e</span><span class="p">.</span><span class="n">is_staff</span><span class="p">)</span>

<span class="c1"># test.py
</span><span class="n">UserFactory</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="n">is_staff</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
</code></pre></div></div>
<p>일부 요소들은 반드시 랜덤으로 돌려서는 안되고 개발자가 반드시 값을 지정해 줘야 할 있다. 이때 <code class="language-plaintext highlighter-rouge">LazyAttribute</code>를 사용한다. 파라미터로 익명 함수(<code class="language-plaintext highlighter-rouge">lambda</code>)를 사용함으로써 개발자가 굳이 별도의 로직을 구현할 필요 없이 입력 데이터를 가공해서 저장할 수도 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">birth_date</span> <span class="o">=</span> <span class="sh">"</span><span class="s">990101</span><span class="sh">"</span>
</code></pre></div></div>

<p>랜덤을 돌리지 않고 그냥 고정시키고 싶다면 이렇게 네이티브 하게 작성하면 된다. 물론, <code class="language-plaintext highlighter-rouge">create</code>에서 필요시 데이터를 변경할 수 도 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ProfileFactory</span><span class="p">(</span><span class="n">factory</span><span class="p">.</span><span class="n">django</span><span class="p">.</span><span class="n">DjangoModelFactory</span><span class="p">)</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">Profile</span>
    <span class="p">...</span> <span class="n">이하</span> <span class="n">생략</span> <span class="bp">...</span>

<span class="k">class</span> <span class="nc">UserFactory</span><span class="p">(</span><span class="n">factory</span><span class="p">.</span><span class="n">django</span><span class="p">.</span><span class="n">DjangoModelFactory</span><span class="p">)</span>
    <span class="k">class</span> <span class="nc">Meta</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">User</span>
    <span class="n">profile</span> <span class="o">=</span> <span class="n">factory</span><span class="p">.</span><span class="nc">SubFactory</span><span class="p">(</span><span class="n">ProfileFactory</span><span class="p">)</span>
</code></pre></div></div>

<p>데이터 저장 시, 관계되어 있는 자식 테이블의 데이터가 필요할 때가 있다. 이때 <code class="language-plaintext highlighter-rouge">factory.SubFactory</code>를 사용한다.</p>

<h2 id="부록-factory_boy에서-사용할-수-있는-필드-리스트">부록) factory_boy에서 사용할 수 있는 필드 리스트</h2>

<table>
  <thead>
    <tr>
      <th>필드명</th>
      <th>설명</th>
      <th>필드명</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>name</td>
      <td>실제 이름</td>
      <td>user_name</td>
      <td>이름(닉네임)</td>
    </tr>
    <tr>
      <td>email</td>
      <td>이메일 주소</td>
      <td>phone_number</td>
      <td>전화번호</td>
    </tr>
    <tr>
      <td>address</td>
      <td>주소</td>
      <td>date</td>
      <td>날짜</td>
    </tr>
    <tr>
      <td>date_time</td>
      <td>날짜 및 시간(<code class="language-plaintext highlighter-rouge">datetime</code>)</td>
      <td>time</td>
      <td>시간</td>
    </tr>
    <tr>
      <td>credit_card_number</td>
      <td>신용카드 번호</td>
      <td>credit_card_expire</td>
      <td>신용카드 만료일</td>
    </tr>
    <tr>
      <td>text</td>
      <td>랜덤 텍스트</td>
      <td>sentence</td>
      <td>1문장</td>
    </tr>
    <tr>
      <td>paragraph</td>
      <td>1문단(여러문장)</td>
      <td>url</td>
      <td>URL</td>
    </tr>
    <tr>
      <td>uuid4</td>
      <td>UUID</td>
      <td>color_name</td>
      <td>색상이름</td>
    </tr>
    <tr>
      <td>random_int(<code class="language-plaintext highlighter-rouge">min: int, max: int</code>)</td>
      <td>랜덤 정수</td>
      <td>random_number(<code class="language-plaintext highlighter-rouge">k</code>)</td>
      <td>K자리 랜덤 숫자</td>
    </tr>
    <tr>
      <td>random_element(<code class="language-plaintext highlighter-rouge">elements: list</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">elements</code> 중에 하나 랜덤</td>
      <td> </td>
      <td> </td>
    </tr>
  </tbody>
</table>]]></content><author><name></name></author><category term="Testing" /><category term="python" /><category term="testing" /><category term="factory_boy" /><summary type="html"><![CDATA[Factory Boy]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">AWS Configure 여러개로 관리하기</title><link href="https://recoma96.github.io/aws/2025/01/15/aws-configure-profile.html" rel="alternate" type="text/html" title="AWS Configure 여러개로 관리하기" /><published>2025-01-15T14:00:00+00:00</published><updated>2025-01-15T14:00:00+00:00</updated><id>https://recoma96.github.io/aws/2025/01/15/aws-configure-profile</id><content type="html" xml:base="https://recoma96.github.io/aws/2025/01/15/aws-configure-profile.html"><![CDATA[<h1 id="개요">개요</h1>

<p>내가 다니고 있는 회사에는 두 개의 서비스를 관리하고 있고, 각 서비스 마다 서로 다른 AWS 계정을 가지고 있다.
나는 두 개의 서비스 중 하나는 전체 관리를 다른 하나는 일부 참여를 하고 있기 때문에 두 개의 AWS 계정을 사용해야 했다.
개발을 로컬에서 하기 때문에 어쩔 수 없이 AWS Configure를 사용해야 하는데, 매번 다른 서비스를 개발할 때마다 일일이 AWS 정보를 입력해야 하는게 여간 귀찮은게 아니다. 
그래서 리서치 결과 <code class="language-plaintext highlighter-rouge">aws configure profile</code>이라는 게 있다는 것을 알았다.</p>

<h1 id="aws-configure-profile">aws configure –profile</h1>

<p>프로필을 생성방법은 아래와 같다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws configure <span class="nt">--profile</span> &lt;ProfileName&gt;
AWS Access Key ID <span class="o">[</span><span class="k">********************</span><span class="o">]</span>:
AWS Secret Access Key <span class="o">[</span><span class="k">********************</span><span class="o">]</span>:
Default region name <span class="o">[</span>ap-northeast-2]:
Default output format <span class="o">[</span>json]:
</code></pre></div></div>

<p><br /></p>

<p>그리고 아래와 같은 명령어로 s3를 불러올 수 있다.</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws s3 <span class="nb">ls</span> <span class="nt">--profile</span> &lt;ProfileName&gt;
</code></pre></div></div>

<p><br /></p>

<p>아래의 명령어로 프로파일의 정보를 출력할 수 있다. 아, 물론 엑세스 키와 시크릿 키는 당연히 마스킹 되어 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ aws configure list

      Name                    Value             Type    Location
      ----                    -----             ----    --------
   profile                &lt;not set&gt;             None    None
access_key     ****************xxxx shared-credentials-file    
secret_key     ****************xxxx shared-credentials-file    
    region           ap-northeast-2      config-file    ~/.aws/config
</code></pre></div></div>

<p>그런데 Pycharm 또는 VSCode IDE에서 사용할 경우 어느 Profile을 사용할 건지 고정을 하고 사용해야 한다. 이때는 환경변수로 세팅을 해서 사용한다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">AWS_DEFAULT_PROFILE</span><span class="o">=</span>&lt;ProfileName&gt;
</code></pre></div></div>]]></content><author><name></name></author><category term="AWS" /><category term="aws" /><category term="iam" /><category term="aws_configure" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">IAM Role이란?</title><link href="https://recoma96.github.io/aws/2025/01/15/iam-role.html" rel="alternate" type="text/html" title="IAM Role이란?" /><published>2025-01-15T08:30:00+00:00</published><updated>2025-01-15T08:30:00+00:00</updated><id>https://recoma96.github.io/aws/2025/01/15/iam-role</id><content type="html" xml:base="https://recoma96.github.io/aws/2025/01/15/iam-role.html"><![CDATA[<h1 id="개요">개요</h1>

<blockquote>
  <p>IAM</p>
</blockquote>

<p>AWS 입문을 해 봤다면 <strong>IAM</strong> 이라는 단어는 자주 들어봤을 것이다. IAM은 사용자 또는 AWS 사용 권한에 대한 관리를 하는 웹 서비스로
보통 사용자를 생성할 때 해당 서비스를 사용하는 것을 볼 수가 있다. IAM 에도 여러가지 세분화가 되어 있는데. 이중에 IAM User는 사용자를 관리하는 항목이다.</p>

<h2 id="iam-user와-aws-configure를-이용한-aws-접근">IAM User와 AWS Configure를 이용한 AWS 접근</h2>

<p>보통 웹 콘솔 말고도 내 컴퓨터나, 서버단에서 AWS 서비스를 사용해야 할 때가 있다. 이 AWS 서비스들을 사용하기 위해 사용자 인증을 하게 되는데 이때 사용되는 툴이 AWS Configure이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws configure
AWS Access Key ID <span class="o">[</span><span class="k">****************</span><span class="o">]</span>:
AWS Secret Access Key <span class="o">[</span><span class="k">****************</span><span class="o">]</span>:
... 이하 생략 ...
</code></pre></div></div>

<p>IAM에서 유저를 생성하면 그 유저에 대한 엑세스 키 아이디(Access Key ID)와 시크릿 엑세스 키(Secret Access Key)를 발행하게 되는데, 이 두개를 AWS Configure에 입력을 해야 비로소
로컬 또는 특정 EC2 서버에서 AWS CLI를 통해 웹 콘솔 없이 AWS 리소스에 접근할 수 있다. 백엔드 서버에서 모듈로 사용하는 경우, 해당 모듈을 사용하기 전에 이 두개를 등록을 해 줘야 한다. 그런데, 
로컬은 그렇다 쳐도 <strong>EC2에서 이렇게 AWS Configure로 유저 정보를 저장하면 발생하는 문제점이 몇가지 있다.</strong></p>

<ol>
  <li>파일에 저장하는 방식이기 때문에 정보가 유출될 가능성이 있다.</li>
  <li>엑세스 키에 기한이 설정되어 있는 경우, 만료됬을 때 AWS 서비스를 사용하는 프로덕트가 정상적으로 작동이 안될 수 있다.</li>
  <li>실무에서 AWS 정보의 주인이 퇴사를 하게 되면, AWS 정보를 바꿔야 하는 번거로움이 발생하게 된다.</li>
</ol>

<p>이러한 문제점 때문에 EC2에서 AWS Configure를 사용하지 않고도 AWS를 접근할 수 있는 대안이 생기게 되는데 이게 바로 <strong>IAM Role(IAM 역할)</strong> 이다.</p>

<h1 id="iam-role">IAM Role</h1>

<p>IAM User가 사용자의 권한을 의미한다면 <strong>IAM Role은 특정 AWS 서비스에서 다른 AWS 서비스에 접근하기 위한 권한을 관리하는 항목이다.</strong> 그러니까, IAM User가 “사람 대 서비스” 였다면, IAM Role은 “서비스 대 서비스” 라고 보면 된다. 예를 들어 아까 위에서 설명했던 EC2에서 S3를 접근하고 싶다고 할때, S3를 접근하는 IAM Role를 생성한 다음, 그 IAM Role을 EC2에 부착하면 된다.</p>

<h2 id="사용법">사용법</h2>

<p>이번 IAM Role 목표는, EC2에서 aws-cli로 S3에 있는 버킷과 파일들을 접근하기 위해 Role을 생성해서 EC2에 붙이려고 한다. (aws-cli는 설치가 되어 있다고 가정한다. <a href="https://docs.aws.amazon.com/ko_kr/cli/latest/userguide/getting-started-install.html">aws-cli 설치방법</a>)</p>

<h3 id="role-생성">Role 생성</h3>

<p>IAM에 들어가서 IAM resources의 Roles를 클릭한다.
<img src="/assets/img/20250115/iam-role-use-1.png" alt="" /></p>

<p><br /></p>

<p>새 Role을 만들어야 하니 Create Role을 선택한다.
<img src="/assets/img/20250115/iam-role-use-2.png" alt="" /></p>

<p><br /></p>

<p>첫번째 스탭으로 누가 이 Role을 사용하는지 선택한다. 우리는 EC2에서 S3에 접근하는 것이 목표이기 때문에 EC2를 고른다.
<img src="/assets/img/20250115/iam-role-use-3.png" alt="" /></p>

<p><br /></p>

<p>이번엔 어느 서비스를 접근하는지 골라야 한다. 우리는 S3에 있는 데이터들을 읽는게 목표이기 때문에 AmazonS3ReadOnlyAccess 를 선택한다.</p>

<p><img src="/assets/img/20250115/iam-role-use-4.png" alt="" /></p>

<blockquote>
  <p>S3에 있는 데이터들을 <strong>읽기</strong> 만 하기 때문에 가급적 ReadOnly 권한을 설정해 줘야 한다. <strong>왜냐하면 IAM에서 권한을 줄 때 딱 필요한 권한만 주는게 원칙이기 때문이다.</strong> 사실 특정 버킷을 의미하는 Resource도 모든 리소스를 조회하는 “*“가 아닌 조회하고자 하는 특정 버킷들의 arn만 작성해야 하지만 여기까지 하면 너무 길어지니 생략.</p>
</blockquote>

<p><br /></p>

<p>이제 Role의 이름만 지어주면 된다. 나는 example-for-ec2로 이름지었다.</p>

<p><img src="/assets/img/20250115/iam-role-use-5.png" alt="" /></p>

<p><br /></p>

<p>다시 IAM Role로 돌아가 보면 생성이 되어있음을 알 수 있다.</p>

<p><img src="/assets/img/20250115/iam-role-use-6.png" alt="" /></p>

<p><br /></p>

<h3 id="ec2에-붙여넣기">EC2에 붙여넣기</h3>

<p>EC2 인스턴스 중에 IAM Role이 적용되지 않는 인스턴스를 선택한 다음 Actions &gt; Security &gt; Modify IAM Role을 선택한다.</p>

<p><img src="/assets/img/20250115/iam-role-use-7.png" alt="" /></p>

<p><br /></p>

<p>여기서 아까 만들었던 Role을 선택한다.</p>

<p><img src="/assets/img/20250115/iam-role-use-8.png" alt="" /></p>

<p><br /></p>

<p>다시 돌아가 보면 IAM Role이 부착되었음을 알 수 있다.</p>

<p><img src="/assets/img/20250115/iam-role-use-9.png" alt="" /></p>

<p><br /></p>

<p>IAM Role이 부착된 EC2를 콘솔로 접근한 다음 아래와 같은 명령어를 입력해 보자 S3 버킷 리스트를 불러올 수 있다면 정상적으로 작동이 된 것이다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ubuntu@ip-127-00-00-000:~<span class="nv">$ </span>aws s3 <span class="nb">ls
</span>2024-01-26 09:06:37 bucket1
2023-11-29 11:01:03 bucket2
2024-01-26 04:57:58 bucket3
</code></pre></div></div>

<p>만약 접근이 안된다면 아래와 같은 결과가 나온다.</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ubuntu@ip-127-00-00-000:~<span class="nv">$ </span>aws s3 <span class="nb">ls

</span>An error occurred <span class="o">(</span>AccessDenied<span class="o">)</span> when calling the ListBuckets operation: User: arn:aws:sts::00000000000:assumed-role/example-for-ec2/i-000000000000 is not authorized to perform: s3:ListAllMyBuckets because no identity-based policy allows the s3:ListAllMyBuckets action
</code></pre></div></div>

<p>해석을 하자면 s3 버킷 리스트를 불러오기 위해서는 <code class="language-plaintext highlighter-rouge">s3:ListAllMyBuckets</code> 라는 권한이 없기 때문에 불러올 수 없다는 뜻이 된다. 이럴 때는 IAM Role이 제대로 부착되어 있는지 확인하고, 부착이 되어 있더라도 <code class="language-plaintext highlighter-rouge">Actions</code>에 <code class="language-plaintext highlighter-rouge">s3:ListAllMyBuckets</code>가 제대로 명시되어 있는지 다시 한번 확인하자.</p>

<h2 id="이밖의-다른-사용법들">이밖의 다른 사용법들</h2>

<p>위에 서술하다시피, IAM Role은 AWS를 사용하는 백엔드가 구축된 EC2 또는 ECS 컨테이너에서 IAM User 대용으로 사용하기도 하지만 이 외에도 여러 용도가 있다.</p>

<h3 id="cloudwatch-logging">Cloudwatch Logging</h3>

<p>서버를 운영하다 보면 무슨 이슈가 발생했다거나 뭔가를 확인하기 위해 로그를 살펴볼 때가 있다. 그런데 로그는 보통 서버 인스턴스 내부에서 출력이 된다. 그렇다고 해서 일일이 EC2 컨테이너에 들어가기에는 많이 번거롭기 때문에 EC2에 있는 로그들을 Cloudwatch로 내보내서 웹상에서 로그를 볼 수 있게 작업을 하는 경우가 있는데, <strong>이는 EC2에서 Cloudwatch라는 서비스를 사용하는, 즉 “서비스 대 서비스”가 되기 때문에 이때 IAM Role이 사용된다.</strong> 정확히는 EC2의 IAM Role에 Cloudwatch 관련 권한을 부여하는 것이다. 이때 권한명은 CloudWatchLogsFullAccess 이다.</p>

<h3 id="aws-lambda">AWS Lambda</h3>

<p><img src="/assets/img/20250115/iam-role-use-10.png" alt="" /></p>

<p>Serverless API 서비스로 유명한 AWS Lambda는 <strong>일단 만들기만 하면 IAM Role이 자동으로 생성된다.</strong> AWS Lambda는 말 그대로 Serverless 이므로 EC2 처럼 프린팅 해서 콘솔상에서 볼 수 있는 방법이 없기 때문에 바로 자동으로 Cloudwatch와 연동이 되는 것이다. 그래서 AWS Lambda에서 로직을 작성할 때 로그를 출력하는 코드를 작성하고 실행을 하면, 그 출력 정보는 Cloudwatch에 바로 기록이 된다.</p>

<h1 id="마치며">마치며</h1>

<p>IAM User가 각각 사용자들의 권한을 관리하는 서비스라면, IAM Role은 서비스 간의 연동 및 사용에 관한 권한을 관리하는 서비스로, AWS 서비스를 관리함에 있어 상당히 비중이 있는 항목이다. 그렇기 때문에 IAM Role에 대해서 공부해 보고 실제로 적용을 하면, AWS를 관리함에 있어 상당히 유용할 수 있다.</p>]]></content><author><name></name></author><category term="AWS" /><category term="aws" /><category term="iam" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Pytest에서 테스트 처음과 끝부분에 프로세스를 작성하는 법</title><link href="https://recoma96.github.io/testing/2025/01/13/pytest-sessionstart-sessionfinish.html" rel="alternate" type="text/html" title="Pytest에서 테스트 처음과 끝부분에 프로세스를 작성하는 법" /><published>2025-01-13T08:30:00+00:00</published><updated>2025-01-13T08:30:00+00:00</updated><id>https://recoma96.github.io/testing/2025/01/13/pytest-sessionstart-sessionfinish</id><content type="html" xml:base="https://recoma96.github.io/testing/2025/01/13/pytest-sessionstart-sessionfinish.html"><![CDATA[<h1 id="개요">개요</h1>

<p>이전에 <a href="https://recoma96.github.io/django/2024/11/22/django-unittest-set-migration-with-other-db.html">DJango에서 테스트 코드 작성 시 마이그레이션을 하지 말아야 할 데이터베이스 사용법</a> 에서 <code class="language-plaintext highlighter-rouge">DiscoverRunner</code>를 사용하면 맨 처음과 맨 마지막의 로직을 작성함으로써 테스트 시작 전 여러 세팅들을 할 수 있다고 언급했다. 그렇다면, pytest에도 이러한 툴이 있을 까?</p>

<h1 id="fixture">fixture</h1>

<p>이번에 설명할 툴에 대해서 언급하기 전에 fixture부터 설명해야 할 필요가 있다. pytest에서의 fixture는 테스트 함수 간에 공유할 수 있는 상태나 데이터를 제공하는 기능이다. 예를 들어 테스트를 하면서 유저 정보가 담긴 Dict 형태의 데이터가 사전에 필요하다면 아래와 같이 구현하면 된다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nd">@fixture</span>
<span class="k">def</span> <span class="nf">user</span><span class="p">():</span>
    <span class="n">user_data</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">recoma</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">birthday</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">19990101</span><span class="sh">"</span><span class="p">}</span>
    <span class="nf">insert_to_database</span><span class="p">(</span><span class="n">user_data</span><span class="p">)</span>
    
    <span class="k">yield</span> <span class="n">user_data</span>

    <span class="nf">remove_from_database</span><span class="p">(</span><span class="n">user_data</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">test_about_user</span><span class="p">(</span><span class="n">user</span><span class="p">:</span> <span class="nb">dict</span><span class="p">):</span>
    <span class="c1"># Do something
</span></code></pre></div></div>

<p>해당 테스트 코드의 순서는 아래와 같다.</p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">test_about_user()</code>가 작동하기 전에 <code class="language-plaintext highlighter-rouge">user()</code>가 작동한다.</li>
  <li><code class="language-plaintext highlighter-rouge">user()</code>에서 user_data를 리턴한다. 이 리턴된 데이터는 <code class="language-plaintext highlighter-rouge">test_about_user</code>의 <code class="language-plaintext highlighter-rouge">user</code>라는 파라미터에 들어간다.</li>
  <li><code class="language-plaintext highlighter-rouge">test_about_user()</code>는 <code class="language-plaintext highlighter-rouge">user</code>를 이용해서 특정 테스트를 진행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">test_about_user()</code>의 프로세스가 끝나면 <code class="language-plaintext highlighter-rouge">user()</code>의 <code class="language-plaintext highlighter-rouge">yield</code>의 아랫부분이 작동한다.</li>
</ol>

<p><br /></p>

<p>이렇게 fixture는 테스팅을 하기 위해 필요한 데이터들을 미리 세팅하고 테스트 함수들에게 그 데이터를 제공하는데 유용하게 쓰인다. 이런 특성을 이용해 테스트를 세팅할 때도 사용되곤 한다.</p>

<h2 id="한계">한계</h2>

<p>그러나 이 fixture 사용에도 한계가 있다.</p>

<h3 id="scope-범위">scope 범위</h3>

<p>fixture에는 scope라고 해서 작동 범위를 설정하는 파라미터가 있다. 선언 방법은 아래와 같다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@fixture</span><span class="p">(</span><span class="n">scope</span><span class="o">=</span><span class="sh">"</span><span class="s">function</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>이는 매 테스트 함수(케이스)가 작동될 때마다 해당 함수가 작동됨을 의미한다. scope를 <code class="language-plaintext highlighter-rouge">module</code>로 설정 할 경우 테스트 파일 내 에서만 작동이 되고 <code class="language-plaintext highlighter-rouge">session</code>으로 설정하면 모든 테스트 파일에서 딱 한번만 작동한다. 일단 <code class="language-plaintext highlighter-rouge">module</code>과, <code class="language-plaintext highlighter-rouge">function</code>은 전체 테스트 에서 딱 한번 실행되는게 아니기 때문에 사용하는데 한계가 있다. <code class="language-plaintext highlighter-rouge">session</code>이 그나마 쓸만해 보이지만 이건 이거대로 문제가 있는데. <code class="language-plaintext highlighter-rouge">session</code>으로 설정된 fixture를 사용하기 위해, <strong>매 함수 또는 일부 함수의 파라미터에다가 해당 fixture를 명시해야 한다.</strong> 단순해 보이지만 이는 일일이 코드가 하나씩 추가됨으로써 클린 코드 상 좋지 못한 결과를 보여줄 수 있다.</p>

<h1 id="pytest_session">pytest_session*</h1>

<p>그렇기 때문에 pytest에서는 아예 테스트 실행과 끝나는 시점에 로직을 작성할 수 있도록 인터페이스를 제공해 주는 데 이게 바로 <code class="language-plaintext highlighter-rouge">pytest_sessionstart</code>와 <code class="language-plaintext highlighter-rouge">pytest_sessionfinish</code>이다. 이 둘을 사용하면 <code class="language-plaintext highlighter-rouge">fixture</code>와 각 테스트 함수에 일일이 설정하지 않아도 알아서 잘 돌아간다. 이 두 함수들은 말 그대로 맨 처음에서 부터 시작하기 때문에 특정 테스트 파일에 들어가기 보다는 <code class="language-plaintext highlighter-rouge">conftest.py</code>에 작성이 되는 경우가 더 많다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># conftest.py
</span>
<span class="k">def</span> <span class="nf">pytest_sessionstart</span><span class="p">(</span><span class="n">session</span><span class="p">:</span> <span class="n">_pytest</span><span class="p">.</span><span class="n">main</span><span class="p">.</span><span class="n">Session</span><span class="p">):</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Start</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">pytest_sessionfinish</span><span class="p">(</span><span class="n">session</span><span class="p">:</span> <span class="n">_pytest</span><span class="p">.</span><span class="n">main</span><span class="p">.</span><span class="n">Session</span><span class="p">):</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Finish!</span><span class="sh">"</span><span class="p">)</span>


<span class="c1"># test_example.py
</span>
<span class="k">def</span> <span class="nf">test_example</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Testing</span><span class="sh">"</span><span class="p">)</span>

</code></pre></div></div>]]></content><author><name></name></author><category term="Testing" /><category term="python" /><category term="testing" /><category term="pytest" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Base64를 사용하는 이유</title><link href="https://recoma96.github.io/computer_science/2024/12/27/base64.html" rel="alternate" type="text/html" title="Base64를 사용하는 이유" /><published>2024-12-27T09:00:00+00:00</published><updated>2024-12-27T09:00:00+00:00</updated><id>https://recoma96.github.io/computer_science/2024/12/27/base64</id><content type="html" xml:base="https://recoma96.github.io/computer_science/2024/12/27/base64.html"><![CDATA[<h1 id="개요">개요</h1>

<p>회사에서 개발과 관련된 일을 하다 보면 OpenAPI를 이용한 기능을 구현할 때가 있다. 그런데 간혹 공공기관의 OpenAPI를 이용하다 보면 대부분 인증 관련 데이터를 Base64로 인코딩을 해달라는 요구가 있다. 심지어 운전면허 검증 API중의 하나인 RIMS API의 경우 body 마저도 AES 암호화에 Base64까지 인코딩을 해야 한다. 아니 어차피 HTTPS로 암호화를 따로 할 텐데 왜 도대체 이런 짓들을 하는 걸까? Base64의 의미와 사용하는 이유를 알아보자.</p>

<h1 id="base64">Base64</h1>

<h2 id="설명">설명</h2>

<p>Base64란 바이너리 데이터를 문자로 64진법 형식으로 인코딩을 하는 방식을 말한다.</p>

<table>
  <thead>
    <tr>
      <th>M</th>
      <th>a</th>
      <th>n</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>01001101</td>
      <td>01100001</td>
      <td>01101110</td>
    </tr>
  </tbody>
</table>

<p>예를 들어 “Man”을 Base64로 인코딩한다고 하자, 문자(char)의 데이터 크기는 1byte, 즉 8bit이다. Man이라는 문자열의 길이는 총 3byte이므로 8bit + 8bit + 8bit = 24bit이다. Base64는 이걸 64진법으로 인코딩한다고 했는데. 64는 2^6이다. 즉, 24bit를 6bit 단위로 쪼갠 다음, Base64 색인표에 따라 텍스트를 생성한다.<br /><br /></p>

<p>출처: 위키백과
<img src="/assets/img/20241227/base64-index.png" alt="base64-index" /></p>

<table>
  <thead>
    <tr>
      <th>T</th>
      <th>W</th>
      <th>F</th>
      <th>u</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>010011</td>
      <td>01<strong>0110</strong></td>
      <td><strong>0001</strong>01</td>
      <td>101110</td>
    </tr>
  </tbody>
</table>

<p>이렇게 해서 “Man”에서 “TWFu”로 인코딩이 되었다.</p>

<h3 id="마지막을-로-패딩하는-이유">마지막을 =로 패딩하는 이유</h3>

<p>가끔씩 보면 마지막이 “=”로 끝나는 경우가 있다. 8bit단위에서 6bit단위로 쪼개기 때문에 마지막에 2bit 혹은 4bit가 비게 되는데. 이때는 “=” 기호로 padding을 하게 된다. 예를 들어 <code class="language-plaintext highlighter-rouge">hello world</code>를 base64로 인코딩을 하면 <code class="language-plaintext highlighter-rouge">aGVsbG8gd29ybGQ=</code> 로 나온다. <code class="language-plaintext highlighter-rouge">hello world</code>라는 문자의 길이는 11byte = 88bit이고 이를 6bit로 쪼개면 마지막에 2bit가 비게 되는데, 이 2bit를 대신 <code class="language-plaintext highlighter-rouge">=</code>로 채운 것이다.</p>

<h2 id="단점">단점</h2>

<p>아까 보다시피 “Man”에서 “TWFu”로 인코딩이 되었는데, 3글자에서 4글자로 1글자 더 추가되었다. 비트를 쪼개는 단위가 8bit에서 6bit로 줄면서 그만큼 글자가 늘어났기 때문이다. 즉, Base64 방식은 데이터의 길이가 늘어난다는 단점이 생기게 된다. 데이터가 커질 수록 스토리지에 저장한다던가, 아니면 데이터 통신을 하는데 있어서 상대적으로 부담이 커질 수 밖에 없다. 그럼에도 불구하고 왜 자주 사용을 할까?</p>

<h2 id="사용-이유">사용 이유</h2>

<p><strong>바이너리 데이터를 손실없이 전송할 수 있기 때문이다.</strong> 데이터 중에서는 우리가 알아볼 수 있는 문자 데이터 말고도 바이너리 데이터에 해당되는 그림 파일, 동영상 파일 등이 있다. <strong>이러한 바이너리 데이터들중 일부는 ASCII 코드에 해당하지 않는 경우가 있다.</strong> 그 이유는 문자(char)의 길이는 8bit(1byte)이지만, ASCII의 범위는 0부터 127까지로 7bit이기 때문이다. 즉, 128부터 255 까지는 ASCII 코드에 해당하지 않기 때문에, 수신자는 이 데이터들을 해석하지 못하게 된다. 하지만 <strong>Base64 인코딩을 사용해 바이너리 데이터들을 ASCII형태의 문자열로 변환을 하게 된다면, 수산자는 해당 데이터를 바로 해석을 할 수 있고, 그 이후에 Base64를 포함한 후속 작업을 진행할 수 있게 된다.</strong></p>

<h3 id="예시-이메일-전송시-사용">예시: 이메일 전송시 사용</h3>

<p>이러한 대표적인 예로 메일을 주고받을 때 사용되는 STMP 프로토콜이 있다. 이 프로토콜은 7bit 단위의 데이터만 취급하기 때문에 8bit 이상의 유니코드나, 기타 바이너리 데이터들은 Base64로 6bit 단위로 인코딩을 해서 송수신을 한다.</p>

<h1 id="utf-8-과의-차이점">UTF-8 과의 차이점</h1>

<p>찬혹 UTF-8 인코딩과 헷갈릴 수 있는데 UTF-8은 <em>Unicode Transformation Format - 8bit</em> 의 약자다. 즉, 유니코드를 표현하기 위해 사용되는 인코딩 방식으로, 다양한 언어를 표현할 때 사용된다. 안전성을 목적으로 둔 Base64와는 경우가 다르다.</p>

<h1 id="파이썬에서-base64-사용해-보기-feat-utf-8">파이썬에서 Base64 사용해 보기 (feat. UTF-8)</h1>

<p>파이썬에서는 base64로 인코딩 또는 디코딩을 하는 모듈을 기본으로 제공한다.</p>

<h2 id="인코딩">인코딩</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">base64</span>

<span class="n">text</span> <span class="o">=</span> <span class="sh">'</span><span class="s">안녕 월드</span><span class="sh">'</span>
<span class="n">utf_text</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">utf_text</span><span class="p">)</span>
<span class="c1"># 바이너리로 변환 -&gt; b'\xec\x95\x88\xeb\x85\x95 \xec\x9b\x94\xeb\x93\x9c'
</span>
<span class="n">encoded_text</span> <span class="o">=</span> <span class="n">base64</span><span class="p">.</span><span class="nf">b64encode</span><span class="p">(</span><span class="n">utf_text</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">encoded_text</span><span class="p">)</span>
<span class="c1"># b'7JWI64WVIOyblOuTnA=='
</span></code></pre></div></div>

<h2 id="디코딩">디코딩</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">base64</span>

<span class="n">encoded_text</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">'</span><span class="s">7JWI64WVIOyblOuTnA==</span><span class="sh">'</span>
<span class="n">decoded_text</span> <span class="o">=</span> <span class="n">base64</span><span class="p">.</span><span class="nf">b64decode</span><span class="p">(</span><span class="n">encoded_text</span><span class="p">)</span>
<span class="c1"># base64 디코딩 -&gt; b'\xec\x95\x88\xeb\x85\x95 \xec\x9b\x94\xeb\x93\x9c'
</span>
<span class="n">text</span> <span class="o">=</span> <span class="n">decoded_text</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
<span class="c1"># utf-8 디코딩 -&gt; 안녕 월드
</span></code></pre></div></div>]]></content><author><name></name></author><category term="Computer_Science" /><category term="computer-science" /><category term="data" /><category term="encoding" /><summary type="html"><![CDATA[개요]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://recoma96.github.io/" /><media:content medium="image" url="https://recoma96.github.io/" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>